# Agent Orchestration Implementation Evidence Map

Source plan:
`C:\Users\涔濇€漒AppData\Roaming\reasonix\global-workspace\docs\agent-orchestration-system-full-plan.md`

Status: strict 1-210 implementation-audit baseline complete.
This remains an evidence map, with live-provider, live-desktop, VM, and production-hardening items tracked separately below.

Last verified in this workspace:

- `corepack pnpm typecheck`
- `corepack pnpm lint`
- `corepack pnpm tsx` across the 24 implementation-audit smoke scripts (`implementedBaselineSections=210`)
- `corepack pnpm vitest run` (`29 passed test files; 376 passed, 1 skipped tests`)
- `.\node_modules\.bin\tsx.CMD scripts\smoke-nfr-api.ts`
- `.\node_modules\.bin\tsx.CMD scripts\smoke-known-limitations-api.ts`
- `.\node_modules\.bin\tsx.CMD scripts\smoke-prompt-drift-api.ts`
- `.\node_modules\.bin\tsx.CMD scripts\smoke-consensus-api.ts`
- `.\node_modules\.bin\tsx.CMD scripts\smoke-content-safety-api.ts`
- `.\node_modules\.bin\tsx.CMD scripts\smoke-trust-calibration-api.ts`
- `.\node_modules\.bin\tsx.CMD scripts\smoke-budget-control-api.ts`
- `.\node_modules\.bin\tsx.CMD scripts\smoke-prompt-context-management-api.ts`
- `node node_modules/tsx/dist/cli.mjs scripts\smoke-human-collaboration-api.ts`
- `node node_modules/tsx/dist/cli.mjs scripts\smoke-agent-profile-capability-report-api.ts`
- `node node_modules/tsx/dist/cli.mjs scripts\smoke-employee-runtime-loop-api.ts`
- Playwright smoke opened `http://localhost:3001`, switched to Canvas, and verified `Agent Canvas`, `Preflight`, and `No workflow preflight yet.` render. Later Playwright smokes switched to Scheduler, Memory, Context, Models, Tools, Caps, Collab, Governance, Monitor, Config, and Factory Run, verifying `Task Scheduler`, `Create Queue`, `Run Due`, `Memory Center`, `Save Memory`, `Learning Review`, `Context Preview`, `Preview Context`, `Packed Sections`, `Model Control`, `Create Model`, `Create Outlet`, `Preview`, `Tool Control`, `Create CLI`, `Register MCP`, `Software Commands`, `Recent Runs`, `Capability Center`, `Rebuild Index`, `Search Capabilities`, `Recommend`, `Apply`, `Collaboration Center`, `Send Message`, `Write Blackboard`, `Open Conflict`, `Governance Center`, `Create Secret Ref`, `Evaluate Autonomy`, `Approval Queue`, `Audit Log`, `Implementation Audit`, `Source gaps`, `Pending implementation`, `Data Lifecycle`, `Data Lifecycle Status`, `Observability Center`, `Record Metric`, `Create Alert`, `Capture Debug Replay`, `Metric Timeline`, `Notifications`, `ConfigOps Center`, `Capture Version`, `Restore Version`, `Restore Selected Version`, `Export Config`, `Analyze Impact`, `Versions`, `Impact Analyses`, `Computer sessions`, `Observe`, and `Screenshot` render.
- Feature flag, degradation, optimistic-lock, export-package, Agent-retirement, organizational-learning, Meta-Agent, Agent-reputation, SDK/Webhook, Multimodal IO, Realtime Co-edit, Style Guide, Diversity, Agent Interview, Performance Review, User Sovereignty, and Accessibility UI source smokes verified `Feature Flags`, `Feature Flag Status`, `Create Flag`, `Evaluate Flag`, `Offline Degradation`, `Degradation Status`, `Save Degradation`, `Evaluate Degradation`, `Optimistic Edit`, `Start Edit`, `Commit Edit`, `Resolve Latest Conflict`, `Edit Conflicts`, `Share Package`, `Create Package`, `Import Check`, `Share Packages`, `Package Import Checks`, `Retirement`, `Create Retirement`, `Transfer Knowledge`, `Complete Latest`, `Retirement Plans`, `Knowledge Transfers`, `Org Learning`, `Promote team memory`, `Build Org Learning`, `Promote Insight`, `Organizational Knowledge`, `Weekly Org Reports`, `Meta Agent`, `Create Meta Agent`, `Generate Digest`, `Meta Agent Digests`, `Meta Recommendations`, `Reputation`, `Score`, `Rank`, `Add Review`, `Agent Reputation`, `Reputation Leaderboard`, `SDK Access`, `Create SDK Key`, `Create SDK Task`, `Webhook`, `Create Webhook`, `SDK API Keys`, `SDK Tasks`, `Webhooks`, `Multimodal IO`, `Realtime Co-edit`, `Open Realtime Session`, `Acquire Segment Lock`, `Apply Realtime Edit`, `Style Guide`, `No style guide`, `Diversity`, `Analyze Diversity`, `Interview and Testing`, `Interview`, `Review`, `User Sovereignty`, `Apply Override`, `Recent overrides`, `Permanent blacklists`, `Accessibility Contract`, `HTML alt`, `Semantic HTML`, `ARIA names`, and `Color-blind palette`, plus the matching API helpers in Governance/ConfigOps/Memory/Observability/Tool Control/Agent Factory/Collaboration centers. A fresh headless Playwright click-smoke against the already-running 3001 dev server did not hydrate client effects, so it was not used as proof for these slices.
- API smoke checks for `/api/collaboration/messages`, `/api/collaboration/blackboard`, `/api/collaboration/conflicts`, `/api/collaboration/conflicts/:id/escalate`, `/api/collaboration/conflict-escalations`, `/api/recovery-events`, `/api/idempotency-records`, `/api/task-queues`, `/api/task-queue-items`, `/api/task-queues/:id/tick`, `/api/task-schedules`, `/api/task-schedules/run-due`, `/api/security/secrets`, `/api/security/credential-scopes`, `/api/security/sandbox-policies`, `/api/security/audit-logs`, `/api/security/findings`, `/api/approvals`, `/api/implementation-audit`, `/api/data-lifecycle/retention-policies`, `/api/data-lifecycle/retention-policies/evaluate`, `/api/data-lifecycle/storage-quotas`, `/api/data-lifecycle/pii-markers`, `/api/data-lifecycle/pii-markers/scan`, `/api/data-lifecycle/export-manifests`, `/api/feature-flags`, `/api/feature-flags/:id/evaluate`, `/api/feature-flag-evaluations`, `/api/degradation/policies`, `/api/degradation/evaluate`, `/api/degradation/events`, `/api/optimistic-locks`, `/api/optimistic-locks/session`, `/api/optimistic-locks/commit`, `/api/edit-conflicts`, `/api/edit-conflicts/:id/resolve`, `/api/export-packages`, `/api/export-packages/:id/import-check`, `/api/package-import-checks`, `/api/agent-retirement-plans`, `/api/agent-retirement-plans/:id/complete`, `/api/knowledge-transfer-packages`, `/api/organizational-knowledge/insights`, `/api/organizational-knowledge/insights/:id/promote`, `/api/organizational-knowledge/reports`, `/api/meta-agent/profiles`, `/api/meta-agent/digests`, `/api/meta-agent/recommendations`, `/api/meta-agent/recommendations/:id/status`, `/api/agent-reputations`, `/api/agent-reputations/leaderboard`, `/api/agent-reputations/refresh`, `/api/agent-profiles/:id/reputation`, `/api/agent-profiles/:id/reputation-reviews`, `/api/sdk/api-keys`, `/api/sdk/api-keys/:id/revoke`, `/api/sdk/tasks`, `/api/sdk/tasks/:id`, `/api/sdk/tasks/:id/events`, `/api/sdk/agents`, `/api/sdk/agents/:id`, `/api/sdk/memories`, `/api/webhooks/subscriptions`, `/api/webhooks/subscriptions/:id/test`, `/api/webhooks/deliveries`, `/api/multimodal-inputs`, `/api/multimodal-outputs`, `/api/style-guides`, `/api/style-guides/:id/bind-agent`, `/api/style-guide-bindings`, `/api/style-guides/evaluate`, `/api/agent-diversity-profiles`, `/api/agent-diversity-analyses`, `/api/agent-interviews`, `/api/performance-reviews`, `/api/user-overrides`, `/api/prompt-templates`, `/api/agent-profiles/:id/context-preview`, `/api/agent-profiles/:id/environment-preview`, `/api/employee-runs`, `/api/employee-runs/:id/debug-package`, `/api/workflow-runs`, `/api/computer-sessions`, `/api/computer-sessions/:id`, `/api/computer-sessions/:id/observations`, `/api/skills`, `/api/memory-items`, `/api/learning-events`, `/api/playbooks`, `/api/observability/metrics`, `/api/observability/alert-rules`, `/api/observability/alert-events`, `/api/observability/debug-replay`, `/api/observability/agent-health`, `/api/notifications`, `/api/notification-preferences`, `/api/capabilities`, `/api/capabilities/search`, `/api/capabilities/knowledge-graph`, `/api/agent-profiles/:id/capability-recommendations`, `/api/capability-recommendations/:id/apply`, `/api/autonomy/decisions`, `/api/model-gateway/connection-tests`, `/api/model-gateway/route-decisions`, `/api/model-gateway/route-preview`, `/api/cli-profiles`, `/api/cli-runs`, `/api/tool-connections`, `/api/mcp-servers`, `/api/mcp-tools`, `/api/mcp-tool-calls`, `/api/software-profiles`, `/api/software-commands`, `/api/software-command-runs`, `/api/recorded-macros`, `/api/macro-replay-runs`, `/api/config-versions`, `/api/config-versions/:id/apply`, `/api/config-exports`, `/api/config-impact-analyses`, `/api/workflow-preflights`, `/api/decision-audit-trails`, `/api/agent-diary-entries`, `/api/continuation-plans`, `/api/continuation-plans/enqueue-due`
- Maintenance API smoke checks cover `/api/maintenance/update-policy`, `/api/maintenance/update-check`, `/api/maintenance/state`, `/api/maintenance/windows`, and `/api/maintenance/windows/:id/complete`.
- Custom metric API smoke checks cover `/api/custom-metrics/profiles`, `/api/custom-metrics/profiles/:id/evaluate`, and `/api/custom-metrics/evaluations`.
- Workflow preset API smoke checks cover `/api/workflow-presets`, `/api/workflow-presets/:id/install`, and `/api/workflow-presets/:id/run`.
- Onboarding API smoke checks cover `/api/onboarding/sessions`, `/api/onboarding/sessions/:id/configure-agent`, `/api/onboarding/sessions/:id/demo`, and `/api/onboarding/sessions/:id/complete`.
- Dynamic permission API smoke checks cover `/api/autonomy/dynamic-permissions`, `/api/autonomy/dynamic-permissions/:id/revoke`, and `/api/autonomy/dynamic-permissions/downgrade`.
- Voice interface API smoke checks cover `/api/voice-interface/profiles` and `/api/voice-interface/conversation-turns`.
- E2E encryption API smoke checks cover `/api/e2e-encryption/policies` and `/api/e2e-encryption/checks`.
- Concurrency model API smoke checks cover `/api/concurrency/profiles` and `/api/concurrency/evaluations`.
- Abuse prevention API smoke checks cover `/api/abuse-prevention/policies`, `/api/abuse-prevention/events`, `/api/abuse-prevention/appeals`, `/api/abuse-prevention/appeals/:id/review`, and the implementation-audit promotion of section 147 to `baseline_plus`.
- Future technology reservation API smoke checks cover `/api/future-tech/seed`, `/api/future-tech/interfaces`, `/api/future-tech/radar`, and the implementation-audit promotion of section 148 to `baseline_plus`.
- Commercial strategy API smoke checks cover `/api/commercial/seed`, `/api/commercial/plans`, `/api/commercial/revenue-streams`, `/api/commercial/policy-rules`, and the implementation-audit promotion of section 149 to `baseline_plus`.
- Open-source governance API smoke checks cover `/api/open-source-governance/seed`, `/api/open-source-governance/components`, `/api/open-source-governance/roles`, `/api/open-source-governance/rfcs`, `/api/open-source-governance/rfcs/:id/advance`, and the implementation-audit promotion of section 150 to `baseline_plus`.
- Contributor guide API smoke checks cover `/api/contributor-guide/seed`, `/api/contributor-guide/prerequisites`, `/api/contributor-guide/policies`, `/api/contributor-guide/evaluate`, and the implementation-audit promotion of section 151 to `baseline_plus`.
- Architecture pattern API smoke checks cover `/api/architecture/seed`, `/api/architecture/patterns`, `/api/architecture/interfaces`, and the implementation-audit promotion of section 152 to `baseline_plus`.
- Error code catalog API smoke checks cover `/api/error-codes/seed`, `/api/error-codes`, category filtering for `M`, and the implementation-audit promotion of section 153 to `baseline_plus`.
- Entity state machine API smoke checks cover `/api/state-machines/seed`, `/api/state-machines`, `/api/state-machines/transitions`, `/api/state-machines/evaluate`, and the implementation-audit promotion of section 154 to `baseline_plus`.
- Agent communication protocol API smoke checks cover `/api/agent-communication/protocols/seed`, `/api/agent-communication/messages`, `/api/agent-communication/messages/validate`, and the implementation-audit promotion of section 155 to `baseline_plus`.
- Tool invocation protocol API smoke checks cover `/api/tool-protocol/seed`, `/api/tool-protocol/invocations`, `/api/tool-protocol/results`, and the implementation-audit promotion of section 156 to `baseline_plus`.
- Streaming protocol API smoke checks cover `/api/stream-protocol/seed`, `/api/stream-protocol/events`, `/api/stream-protocol/replay`, and the implementation-audit promotion of section 157 to `baseline_plus`.
- Prompt engineering API smoke checks cover `/api/prompt-engineering/seed`, `/api/prompt-engineering/evaluate` weak/strong prompt validation, and the implementation-audit promotion of section 158 to `baseline_plus`.
- Skill developer SDK API smoke checks cover `/api/skills/sdk/manifests`, `/api/skills/sdk/scaffold`, `/api/skills/sdk/manifests/:id/publish`, `/api/skills/sdk/publications`, and the implementation-audit promotion of section 159 to `baseline_plus`.
- Plugin framework API smoke checks cover `/api/plugins`, `/api/plugins/:id/enable`, `/api/plugins/:id/health-check`, `/api/plugins/:id/compatibility`, `/api/plugins/:id/upgrade`, `/api/plugins/:id/events`, `/api/system/plugins`, extension point registration, lifecycle events, marketplace metadata, health `ok`, compatibility `true`, and the implementation-audit promotion of section 35 to `baseline_plus`.
- Team collaboration API smoke checks cover `/api/team-users`, `/api/teams`, `/api/teams/:id/members`, `/api/team-permissions/evaluate`, `/api/team-resource-shares`, `/api/team-approval-policies`, `/api/team-approval-policies/:id/decisions`, `/api/team-approval-policies/:id/evaluate`, admin/operator/viewer permission differences, model-profile sharing with `user_isolated` secret handling, private memory sharing, all-must-approve resolution from `pending` to `approved`, and the implementation-audit promotion of section 37 to `baseline_plus`.
- System bootstrap API smoke checks cover `/api/system-bootstrap/checks/run`, `/api/system-bootstrap/checks?status=warning`, Section 39 health checks for database, queues, model providers, MCP servers, memory, running Agents, approvals, API latency, WebSocket counts, event throughput, slow queries, checkpoint latency, Ops/Meta Agent availability, and the implementation-audit promotion of section 39 to `baseline_plus`.
- Structured knowledge graph API smoke checks cover `/api/knowledge-graph`, `/api/knowledge-graph/query`, Section 40 node types, `uses`/`depends_on`/`solves`/`causes`/`belongs_to`/`prefers`/`avoids` relation types, deterministic node embeddings, memory evidence, software command success-case evidence, and the implementation-audit promotion of section 40 to `baseline_plus`.
- Simulation/backtesting API smoke checks cover `/api/simulations`, `/api/simulations/:id/review`, `/api/golden-task-sets`, `/api/backtests`, dry-run simulated environments, user review approval, Golden Set tasks, candidate-vs-baseline scoring, CI-style gate status, and the implementation-audit promotion of section 41 to `baseline_plus`.
- Error recovery strategy API smoke checks cover `/api/error-recovery/classify`, `/api/error-recovery/recommend`, `/api/error-recovery/classifications`, `/api/error-recovery/attempts`, `/api/error-recovery/strategy-stats`, taxonomy categories, severity decisions, retry-vs-different-approach success-rate reranking, permission ask-user behavior, model fallback recommendations, and the implementation-audit promotion of section 42 to `baseline_plus`.
- Agent Persona API/runtime/UI smoke checks cover `agent_profiles.persona`, Agent Profile create/patch validation, Agent Factory Persona controls, runtime event persona payloads, deterministic `personaDecisionStyle`, prompt context visibility, and the implementation-audit promotion of section 43 to `baseline_plus`.
- Agent template marketplace API smoke checks cover `/api/agent-templates/seed`, `/api/agent-templates`, `/api/agent-templates/:id/publish`, `/api/agent-templates/:id/install`, `/api/agent-template-installs`, the 20 default employee templates, real Agent Profile creation from an Agent Profile template, record-only v1 Workflow template install, install history, and the implementation-audit promotion of section 44 to `baseline_plus`.
- Technical architecture API smoke checks cover `/api/technical-architecture/manifest`, `/api/technical-architecture/evaluate`, `/api/technical-architecture/evaluations`, Electron+Node selection, React/TypeScript/Zustand/Tailwind detection, custom Canvas/CodeMirror warning, SQLite WAL, event bus, CLI child-process runner, browser/computer sessions, AES-GCM vault, supplemental table mappings, runtime data flow, and the implementation-audit promotion of section 46 to `baseline_plus`.
- Test strategy API smoke checks cover `/api/test-strategy/seed`, `/api/test-strategy/items?kind=chaos_case`, `/api/test-strategy/evaluate`, 3 test-pyramid layers, 16 key integration cases, 3 Mock model capabilities, 4 record-only chaos cases, and the implementation-audit promotion of section 47 to `baseline_plus`.
- Help center API smoke checks cover `/api/help-center/seed`, `/api/help-center/surfaces?surfaceKey=agent_factory`, `/api/help-center/items?surfaceKey=agent_factory&itemType=question_button`, `/api/help-center/onboarding-flows?flowKey=first_agent_success_path`, 10 default surfaces, 40 help items, 1 onboarding flow, Agent Factory question-button lookup, and the implementation-audit promotion of section 48 to `baseline_plus`.
- I18n contract API smoke checks cover `/api/localization/contract/seed`, `/api/localization/contract/checks?area=localized_errors&status=passing`, `/api/localization/contract/evaluate`, `/api/localization/resolve`, all five Section 49 checks passing, persona-language system prompt resolution to `en-US`, and the implementation-audit promotion of section 49 to `baseline_plus`.
- Accessibility profile API smoke checks cover `/api/accessibility/profiles/seed`, `/api/accessibility/profiles?status=active`, `/api/accessibility/profiles/:id/evaluate`, one default profile, five passing accessibility checks, and the implementation-audit promotion of section 50 to `baseline_plus`.
- Future architecture API smoke checks cover `/api/future-architecture/reservations/seed`, `/api/future-architecture/reservations?track=single_machine_to_cluster`, `/api/future-architecture/reservations/evaluate`, six architecture evolution reservations, three cluster migration reservations, four tracks, and the implementation-audit promotion of section 51 to `baseline_plus`.
- OS interference API smoke checks cover `/api/os-interference/policies/seed`, `/api/os-interference/evaluate`, `/api/os-interference/events?status=needs_user`, UAC notify-user handling, native-file-picker CLI/API bypass, screen-lock pause, RDP disconnect UI pause, low-battery pause, no-interference continue, and the implementation-audit promotion of section 89 to `baseline_plus`.
- File-system boundary API smoke checks cover `/api/file-boundaries/policies/seed`, `/api/file-boundaries/evaluate`, `/api/file-boundaries/evaluations?status=blocked`, UTF-16/BOM/CRLF normalization, Windows MAX_PATH warnings, locked-write user notification, unsafe filename normalization, binary metadata-only routing, huge-file direct-read blocking, and the implementation-audit promotion of section 90 to `baseline_plus`.
- Browser automation trap API smoke checks cover `/api/browser-automation-traps/policies/seed`, `/api/browser-automation-traps/evaluate`, `/api/browser-automation-traps/evaluations?status=needs_user`, extension interference clean-profile handling, zoom/DPI/viewport/rendering stabilization, selector-first and SSIM recommendations, Cloudflare/reCAPTCHA pause-and-notify behavior, user-approved session reuse guidance, and the implementation-audit promotion of section 91 to `baseline_plus`.
- Enterprise network API smoke checks cover `/api/enterprise-network/policies/seed`, `/api/enterprise-network/evaluate`, `/api/enterprise-network/evaluations?status=needs_user`, HTTP/PAC/system proxy guidance, NTLM/Kerberos proxy auth, Secret Vault password refs, Node/browser/Python proxy adapters, noProxy local bypasses, enterprise CA bundle handling, TLS-inspection warnings, and the implementation-audit promotion of section 92 to `baseline_plus`.
- Output consistency API smoke checks cover `/api/output-consistency/policies/seed`, `/api/output-consistency/evaluate`, `/api/output-consistency/evaluations?status=warning`, zh-CN output language enforcement, mixed-language detection, English code-comment requirements, Prettier/Black/gofmt formatter mapping, missing/failed formatter warnings, and the implementation-audit promotion of section 93 to `baseline_plus`.
- Resource governor API smoke checks cover `/api/resource-governor/policies/seed`, `/api/resource-governor/evaluate`, `/api/resource-governor/evaluations?status=paused`, CPU/GPU/memory/disk/network pressure, foreground/background priority effects, battery concurrency caps, low/critical battery checkpoint actions, local-LLM disablement, browser slowdown, thermal pressure, and the implementation-audit promotion of section 94 to `baseline_plus`.
- Global OS integration API smoke checks cover `/api/global-os-integration/policies/seed`, `/api/global-os-integration/evaluate`, `/api/global-os-integration/evaluations?status=delayed`, virtual clipboard use, direct input dispatch, system clipboard backup/restore, active-user focus delay, headless/background preference, native file-input injection, PDF generation, CSS color injection, unknown-dialog user assistance, and the implementation-audit promotion of section 95 to `baseline_plus`.
- Telemetry policy API smoke checks cover `/api/telemetry/policies/seed`, `/api/telemetry/policies`, `/api/telemetry/events/evaluate`, `/api/telemetry/events`, `/api/telemetry/export`, and `/api/telemetry/exports`, default off/explicit-consent behavior, aggregate-only usage metrics, performance metric scoping, full-level redaction, sensitive-only blocking, sanitized export manifests, and the implementation-audit promotion of section 97 to `baseline_plus`.
- Model invocation optimization API smoke checks cover `/api/model-optimization/policies/seed`, `/api/model-optimization/policies`, `/api/model-optimization/cache/evaluate`, `/api/model-optimization/cache-entries`, `/api/model-optimization/parameters/resolve`, `/api/model-optimization/warmups`, `/api/model-optimization/warmups/:id/complete`, and `/api/model-optimization/events`, exact and semantic cache hit/miss behavior, no-cache bypasses for fresh tasks, saved-cost stats, task-type parameter resolution, Agent override parameters, record-only warmup sessions, and the implementation-audit promotion of section 99 to `baseline_plus`.
- Runtime micro-operation API smoke checks cover `/api/runtime-micro-operations/policies/seed`, `/api/runtime-micro-operations/policies`, `/api/runtime-micro-operations/timeouts/evaluate`, `/api/runtime-micro-operations/busy/evaluate`, `/api/runtime-micro-operations/decisions`, `/api/runtime-micro-operations/scheduled-actions`, `/api/runtime-micro-operations/scheduled-actions/run-due`, `/api/runtime-micro-operations/inbox`, and `/api/runtime-micro-operations/inbox/process-next`, approval/idle/stuck timeout decisions, busy-task preempt/delegate/queue behavior, delayed-action due/queued handling, priority inbox processing, and the implementation-audit promotion of section 100 to `baseline_plus`.
- Workflow advanced operations API smoke checks cover `/api/workflow-advanced-operations/partial-reruns`, `/api/workflow-advanced-operations/partial-reruns/:id/apply`, `/api/workflow-advanced-operations/task-merge-suggestions`, `/api/workflow-advanced-operations/task-merge-suggestions/:id/decide`, `/api/workflow-advanced-operations/task-merge-suggestions/:id/apply`, and `/api/workflow-advanced-operations/template-instantiations`, partial reruns from a selected node with cached upstream node preservation, user-approvable task merge suggestions, template parameter validation/rendering, new draft workflow instances, and the implementation-audit promotion of section 101 to `baseline_plus`.
- Data maintenance API smoke checks cover `/api/data-maintenance/policies/seed`, `/api/data-maintenance/policies`, and `/api/data-maintenance/runs`, record-only log rotation warnings, SQLite backup/integrity/ANALYZE/VACUUM/REINDEX scheduling, workspace GC candidate detection with artifact/checkpoint/long-term-work preservation, browser profile 500MB warnings, inactive-profile archive candidates, cookie retention, and the implementation-audit promotion of section 102 to `baseline_plus`.
- Agent probation API smoke checks cover `/api/agent-probation`, `/api/agent-probation/evaluate`, `/api/agent-probation/promotions`, `/api/agent-probation/promotions/:id/decide`, and `/api/agent-probation/promotions/:id/apply`, default new-Agent probation records, staging risk restrictions, 10-task/>80% graduation checks, approval-gated production promotion, optional A/B evidence, and the implementation-audit promotion of section 104 to `baseline_plus`.
- Agent experiment API smoke checks cover `/api/agent-profiles/:id/clone`, `/api/agent-comparisons`, `/api/agent-what-if`, semantic-only memory copy, three repeated A/B task rows, five what-if impact items, affected workflow detection, and the implementation-audit promotion of section 105 to `baseline_plus`.
- Knowledge decay API/browser smoke checks cover `/api/memory-decay-snapshots`, `/api/memory-decay-snapshots/:id/actions`, pinned/decaying/expiring-soon point classifications, delete-confirmation planning, pin action application, Memory Center `Knowledge Decay` SVG rendering with clickable points, and the implementation-audit promotion of section 107 to `baseline_plus`.
- Memory integrity API smoke checks cover `/api/memory-integrity/policies/seed`, `/api/memory-integrity/policies`, `/api/memory-integrity/evaluate`, `/api/memory-integrity/scan`, and `/api/memory-integrity/evaluations`, source confidence lowering for external web/task/file knowledge, dangerous-pattern blocking for hardcoded secrets/security bypass instructions, contradiction flagging against high-confidence trusted memories, periodic scan summaries, and the implementation-audit promotion of section 108 to `baseline_plus`.
- Natural-language Workflow API/browser smoke checks cover `/api/workflow-nl-drafts`, `/api/workflow-nl-drafts/:id/confirm`, deterministic GitHub Issue triage parsing, Webhook Trigger -> Agent -> Condition -> Fix Agent / Plan Document preview generation, draft confirmation into real Workflow nodes/edges, Agent Canvas `NL Workflow` panel rendering, and the implementation-audit promotion of section 109 to `baseline_plus`.
- NFR API smoke checks cover `/api/nfr/requirements/seed`, `/api/nfr/requirements`, `/api/nfr/evaluate`, and `/api/nfr/evaluations`, reliability/usability/compatibility/security/maintainability targets, pass/fail/unknown metric evaluation, missing-metric visibility, and the implementation-audit promotion of section 111 to `baseline_plus`.
- Known limitation API smoke checks cover `/api/known-limitations/seed`, `/api/known-limitations`, `/api/known-limitations/evaluate`, `/api/known-limitations/:id/acknowledge`, and `/api/known-limitations/acknowledgements`, all ten v1 limitation disclosures from Section 112, surface/category/severity filtering, capability preflight matching, acknowledgement gating, acknowledgement history, and the implementation-audit promotion of section 112 to `baseline_plus`.
- Prompt drift API smoke checks cover `/api/prompt-drift/monitors`, `/api/prompt-drift/snapshots`, `/api/prompt-drift/checks`, and `/api/prompt-drift/runs`, Section 118 schedules/checks/actions, pinned model behavior snapshots, baseline-vs-candidate metric drift detection for output schema/refusal/verbosity/tool calling/reasoning/latency/cost, stored drift signals, recommended incident action, and the implementation-audit promotion of section 118 to `baseline_plus`.
- Consensus API smoke checks cover `/api/consensus/dual-model-verifications`, `/api/consensus/agent-votes`, and `/api/consensus/adversarial-reviews`, Section 119 critical-task dual-model verification, primary/secondary disagreement points, use-secondary recommendation, quorum/majority Agent voting, red-team adversarial review findings, listing filters, and the implementation-audit promotion of section 119 to `baseline_plus`.
- Content safety API smoke checks cover `/api/content-safety/policies/seed`, `/api/content-safety/policies`, `/api/content-safety/scan`, `/api/content-safety/scans`, and `/api/content-safety/copyright-checks`, Section 120 L1 keyword/regex and PII scanning, L2 local classifier categories, L3 cloud-safety consent gating without live external calls, redact decisions, code similarity copyright checks, image metadata flags, record-only reverse image search requirements, listing filters, and the implementation-audit promotion of section 120 to `baseline_plus`.
- Trust calibration API smoke checks cover `/api/trust-calibration/policies/seed`, `/api/trust-calibration/policies`, `/api/trust-calibration/evaluate`, and `/api/trust-calibration/evaluations`, Section 121 high-confidence indicators, low-confidence warning/uncertainty signals, anti-overtrust streak checks, day-1/day-3/day-7/day-30 trust path mapping, autonomy increase/decrease recommendations, listing filters, and the implementation-audit promotion of section 121 to `baseline_plus`.
- Budget control API smoke checks cover `/api/budget-control/policies/seed`, `/api/budget-control/policies`, `/api/budget-control/evaluate`, `/api/budget-control/evaluations`, and `/api/budget-control/usage-report`, Section 27 per-task/per-Agent-day/per-project-month/global-month policies, USD and token limits, notify thresholds, hard-cap stop-task decisions, soft token warnings, model-routing hints, usage CSV export, and the implementation-audit promotion of section 27 to `baseline_plus`.
- Agent Profile capability report API smoke checks cover `/api/agent-profiles/:id/capability-report`, resolved primary model, Skills, MCP servers, CLI profiles, software commands, permission matrix, output contract summary, readiness score, no-gap ready state, and the implementation-audit promotion of sections 1-3 to `baseline_plus`.
- Employee runtime loop API smoke checks cover `/api/employee-runs/:id`, successful runtime completion, five-step `loopTrace`, per-phase observe/decide/execute/verify trace metadata, `nextRuntimeAction`, recovery-plan fields, and the implementation-audit promotion of section 4 to `baseline_plus`.
- Prompt/context management API smoke checks cover `/api/prompt-templates/:id/render`, `/api/context-compressors/policies/seed`, `/api/context-compressors/policies`, `/api/context-compressors/plan`, and `/api/context-compressors/plans`, template variable rendering, conditional blocks, Prompt A/B metadata, default 80-percent compression policies, preserved/compressed context sections, model-call token allocation, and the implementation-audit promotion of section 31 to `baseline_plus`.
- Human collaboration API smoke checks cover `/api/human-collaboration/approval-policies`, `/api/human-collaboration/approval-policies/:id/evaluate`, `/api/human-collaboration/plan-approvals`, `/api/human-collaboration/takeovers`, `/api/human-collaboration/takeovers/:id/actions`, and `/api/human-collaboration/takeovers/:id/complete`, timeout/escalation/batching/auto-approval policy evaluation, per-step plan approval decisions, linked approval request resolution, takeover action logging, completed takeover history, and the implementation-audit promotion of section 32 to `baseline_plus`.
- Product glossary API smoke checks cover `/api/documentation/glossary/seed`, `/api/documentation/glossary?term=%E6%B5%81%E7%A8%8B`, `/api/documentation/glossary?term=%E5%9B%9E%E6%BB%9A`, the 33 default glossary terms, Section 113 user-facing to internal vocabulary mappings, and the implementation-audit promotion of section 113 to `baseline_plus`.
- Competitive positioning API smoke checks cover `/api/competitive-positioning/seed`, `/api/competitive-positioning/reports?status=active`, the five Section 114 competitor categories, seven differentiator keys, strategic implication areas, custom draft reports, and the implementation-audit promotion of section 114 to `baseline_plus`.
- Ecosystem roadmap API smoke checks cover `/api/ecosystem-roadmap/seed`, `/api/ecosystem-roadmap/phases?stage=ecosystem`, the four Section 115 phases, Phase 1 asset targets for 20 Agent templates, 10 Workflow templates, and 50 Skills, Phase 3 marketplace/SDK/community channels, Phase 4 enterprise readiness, and the implementation-audit promotion of section 115 to `baseline_plus`.
- Ethical alignment API smoke checks cover `/api/ethical-alignment/seed`, `/api/ethical-alignment/evaluate`, `/api/ethical-alignment/evaluations?decision=refused`, the 11 Section 116 refuse categories, six warning categories, privacy/security/transparency user values, uncertainty handling, and the implementation-audit promotion of section 116 to `baseline_plus`.
- Legal compliance API smoke checks cover `/api/legal-compliance/seed`, `/api/legal-compliance/disclaimers?placement=approval_footer`, `/api/legal-compliance/license-checks`, the Section 117 GDPR/CCPA/HIPAA/PIPL matrix, four disclaimer placements, MIT/GPL/unknown license risk outcomes, and the implementation-audit promotion of section 117 to `baseline_plus`.
- Emotional UX API smoke checks cover `/api/emotional-ux/seed`, `/api/emotional-ux/guidelines?guidelineType=tone`, the Section 122 tone, microinteraction, and anxiety-reduction guideline sets, persistent emergency-stop UX, dangerous-action warning guidance, long-silence progress updates, and the implementation-audit promotion of section 122 to `baseline_plus`.
- Test fixture API smoke checks cover `/api/test-fixtures/seed`, `/api/test-fixtures?type=file`, `/api/test-fixtures?type=memory_fixture`, `/api/test-fixtures/:id/generate`, `/api/test-fixture-runs`, and the implementation-audit promotion of section 160 to `baseline_plus`.
- Benchmark suite API smoke checks cover `/api/benchmarks/seed`, `/api/benchmarks/cases?dimension=safety`, `/api/benchmarks/runs`, `/api/benchmarks/runs/:id/results`, and the implementation-audit promotion of section 161 to `baseline_plus`.
- Localization API smoke checks cover `/api/localization/seed`, `/api/localization/resources`, `/api/localization/translate`, `/api/localization/agent-policies`, `/api/localization/resolve`, and the implementation-audit promotion of section 162 to `baseline_plus`.
- Theme profile API smoke checks cover `/api/theme-profiles/seed`, `/api/theme-profiles`, `/api/theme-profiles/resolve`, and the implementation-audit promotion of section 163 to `baseline_plus`.
- Keyboard shortcut API smoke checks cover `/api/keyboard-shortcuts/seed`, `/api/keyboard-shortcuts?scope=canvas`, `/api/keyboard-shortcuts/resolve`, `/api/keyboard-shortcuts/conflicts`, and the implementation-audit promotion of section 164 to `baseline_plus`.
- Reasonix file format API smoke checks cover `/api/reasonix-file-formats/seed`, `/api/reasonix-file-formats?formatKind=debug`, `/api/reasonix-file-formats/validate`, `/api/reasonix-file-validations?formatKind=debug`, valid signed payload validation, recursive secret-reference rejection, and the implementation-audit promotion of section 165 to `baseline_plus`.
- Migration wizard API smoke checks cover `/api/migrations/compatibility`, `/api/migrations/sessions/:id/import`, `/api/migrations/sessions?sourceTool=csv`, `/api/migrations/import-records?sessionId=...`, CSV Agent/Memory import, LangChain dry-run manual mappings, blocked import protection, and the implementation-audit promotion of section 166 to `baseline_plus`.
- Performance analysis API smoke checks cover `/api/performance-analysis/runs`, `/api/performance-analysis/runs?scope=system`, `/api/performance-analysis/recommendations?analysisRunId=...`, P50/P95 latency calculation, slow query/tool/step summaries, automatic optimization recommendations, and the implementation-audit promotion of section 167 to `baseline_plus`.
- Security audit checklist API smoke checks cover `/api/security-audits/checklist/seed`, `/api/security-audits/checklist?cadence=continuous`, `/api/security-audits/runs`, `/api/security-audits/runs?cadence=major_version`, `/api/security-audits/run-items?runId=...`, completed/failed checklist runs, and the implementation-audit promotion of section 168 to `baseline_plus`.
- Incident response API smoke checks cover `/api/incident-response/plans/seed`, `/api/incident-response/plans`, `/api/incident-response/incidents`, `/api/incident-response/incidents?severity=P0`, `/api/incident-response/actions?incidentId=...`, `/api/incident-response/actions/:id/complete`, P0 action generation/completion, and the implementation-audit promotion of section 169 to `baseline_plus`.
- Capacity planning API smoke checks cover `/api/capacity-planning/profiles/seed`, `/api/capacity-planning/profiles`, `/api/capacity-planning/evaluate`, `/api/capacity-planning/evaluations`, standard hardware tiers, storage estimates, over-capacity warnings, and the implementation-audit promotion of section 170 to `baseline_plus`.
- Feature deprecation API smoke checks cover `/api/deprecation-policy/stages/seed`, `/api/deprecation-policy/stages`, `/api/deprecation-policy/features`, `/api/deprecation-policy/features/:id/resolve-stage`, `/api/deprecation-policy/features/:id/migrate`, `/api/deprecation-policy/migration-runs`, the 0/3/6/9 month deprecation stage schedule, dry-run migration records, and the implementation-audit promotion of section 171 to `baseline_plus`.
- Documentation architecture API smoke checks cover `/api/documentation/architecture/seed`, `/api/documentation/sections`, `/api/documentation/pages`, seven docs sections, 27 published page records, category/status filters, release-notes README mapping, actual docs page skeletons, and the implementation-audit promotion of section 172 to `baseline_plus`.
- Glossary API smoke checks cover `/api/documentation/glossary/seed`, `/api/documentation/glossary`, all 16 user/internal term mappings, category filters, Chinese query search for `%E7%86%94%E6%96%AD`, English query search for `dead`, and the implementation-audit promotion of section 173 to `baseline_plus`.
- FAQ API smoke checks cover `/api/documentation/faq/seed`, `/api/documentation/faq`, seven FAQ entries, security category filtering, `Ollama` query matching local/offline FAQ entries, Chinese `%E5%8F%9B%E5%8F%98` query matching the Agent runaway safety FAQ, and the implementation-audit promotion of section 174 to `baseline_plus`.
- Troubleshooting API smoke checks cover `/api/documentation/troubleshooting/seed`, `/api/documentation/troubleshooting`, 11 symptom/cause/solution entries, model category filtering, Chinese browser query search, `Vault` query search, and the implementation-audit promotion of section 175 to `baseline_plus`.
- Quick reference API smoke checks cover `/api/documentation/quick-reference/seed`, `/api/documentation/quick-reference`, seven action cards, agent/safety/debug category filters, Chinese takeover query search, shortcut/step mappings, and the implementation-audit promotion of section 176 to `baseline_plus`.
- Non-goals API smoke checks cover `/api/documentation/non-goals/seed`, `/api/documentation/non-goals`, 14 product boundary policies, 9 `v1_not_do` policies, 5 `never_do` policies, permanent `block_and_audit` enforcement, and the implementation-audit promotion of section 177 to `baseline_plus`.
- Branding API smoke checks cover `/api/branding/seed`, `/api/branding/candidates`, `/api/branding/guidelines`, Chinese and English candidate filters, the slogan `浣犵殑AI鍛樺伐鍥㈤槦锛屾湰鍦拌繍琛宍, tone keywords, avoid-over-personification guidance, and the implementation-audit promotion of section 178 to `baseline_plus`.
- Success metrics API smoke checks cover `/api/success-metrics/seed`, `/api/success-metrics/definitions`, `/api/success-metrics/snapshots`, 17 metric definitions, product/quality/community/business categories, threshold metadata, met/missed/observed snapshot evaluation, and the implementation-audit promotion of section 179 to `baseline_plus`.
- Readiness checklist API smoke checks cover `/api/readiness-checklist/seed`, `/api/readiness-checklist`, eight required pending readiness items, security/documentation category filters, acceptance criteria, and the implementation-audit promotion of section 180 to `baseline_plus`.
- OAuth API smoke checks cover `/api/oauth/credentials`, `/api/oauth/credentials/:id/evaluate-operation`, `/api/oauth/credentials/:id/refresh-failure`, `/api/oauth/credentials/:id/reauthorize`, `/api/oauth/refresh-events`, shared and Agent-scoped credentials, scope and operation allowlists, consent gating, refresh-failure pause metadata, reauthorization recovery, and the implementation-audit promotion of section 181 to `baseline_plus`.
- Workspace initialization API smoke checks cover `/api/workspace-templates`, `/api/workspace-inits`, `/api/workspace-inits/:id/failure`, template and empty sources, setup flags, verification checks, failure policies, and the implementation-audit promotion of section 182 to `baseline_plus`.
- Custom model API smoke checks cover `/api/custom-models`, `/api/custom-models/:id/evaluate`, `/api/custom-models/dataset-exports`, fine-tune source metadata, usage constraints, compatible/incompatible Skills, context-window checks, manifest-only dataset exports, consent status, and the implementation-audit promotion of section 183 to `baseline_plus`.
- Project context API smoke checks cover `/api/project-contexts`, `/api/project-contexts/:id/agent-roles`, `/api/project-switch-events`, project overrides, Agent roles, project-specific memories, switch checkpoint behavior, sequential/parallel/time-sliced modes, and the implementation-audit promotion of section 184 to `baseline_plus`.
- Behavior stabilization API smoke checks cover `/api/behavior-stabilization/snapshots`, `/api/behavior-stabilization/analyze`, `/api/behavior-stabilization/analyses`, `/api/behavior-stabilization/runs`, baseline/current behavior snapshots, maxAllowedDeviation, significant drift classification, stabilization actions, and the implementation-audit promotion of section 185 to `baseline_plus`.
- Skill synthesis API smoke checks cover `/api/skill-synthesis/discover`, `/api/skill-synthesis/records`, `/api/tool-pipelines`, `/api/tool-pipelines/:id/publish`, complementary Skill discovery, ordered tool chains, input/output mappings, failure policy, publishable composite Skill promotion, and the implementation-audit promotion of section 186 to `baseline_plus`.
- Unified search API smoke checks cover `/api/unified-search/index`, `/api/unified-search/query`, cross-entity indexing, keyword/semantic/hybrid modes, scope controls, filtered project/Agent/date/type search, grouped results, highlighted snippets, natural-language query mode, and the implementation-audit promotion of section 187 to `baseline_plus`.
- Context cache API smoke checks cover `/api/context-cache/preload`, `/api/context-cache/resolve`, `/api/context-cache`, code/data/doc/general predictor selection, preload flags, TTL expiry metadata, file-change invalidation, listing filters, and the implementation-audit promotion of section 188 to `baseline_plus`.
- Browser session API smoke checks cover `/api/browser-sessions`, `/api/browser-sessions/:id/evaluate-access`, `/api/browser-sessions/:id/keep-alive`, `/api/browser-sessions/:id/export`, `/api/browser-session-events`, encrypted state references, owner/shared Agent isolation, blocked domains, keep-alive planning, encrypted export manifests, event logs, and the implementation-audit promotion of section 190 to `baseline_plus`.
- Task template API smoke checks cover `/api/task-templates/seed`, `/api/task-templates`, `/api/task-templates/:id/instantiate`, `/api/task-template-runs`, `/api/task-template-runs/:id/complete`, typed parameters, `{{param}}` rendering, default templates, usage stats, and the implementation-audit promotion of section 191 to `baseline_plus`.
- Agent mentorship API smoke checks cover `/api/agent-mentorships`, `/api/agent-mentorships/:id/actions`, `/api/agent-mentoring-events`, mentor/mentee relationships, scope/style/action gates, review/memory/practice/progress events, proficiency tracking, automatic graduation, and the implementation-audit promotion of section 192 to `baseline_plus`.
- Context window visualizer API smoke checks cover `/api/context-window-visualizations`, `/api/context-window-visualizations/:id/actions`, token capacity/used/overflow metadata, segment content types, content-type breakdowns, suggestion actions, compress-plan preview, expand-window preview, listing filters, and the implementation-audit promotion of section 194 to `baseline_plus`.
- Memory graph API smoke checks cover `/api/memory-graph-views`, `/api/memory-graph-views/:id/export`, memory/entity/Agent nodes, `prefers`/`belongs_to`/`depends_on`/`has_error`/`solves`/`similar_to` edges, node/edge counts, export manifests, listing filters, and the implementation-audit promotion of section 195 to `baseline_plus`.
- Agent team dashboard API smoke checks cover `/api/agent-team-dashboard-snapshots`, `/api/agent-team-dashboard-snapshots/:id/commands`, `/api/agent-team-dashboard-commands`, Agent card statuses, shared blackboard summaries, pause-all command records, export-report manifests, command listing, and the implementation-audit promotion of section 196 to `baseline_plus`.
- CI/CD integration API smoke checks cover `/api/cicd/integrations`, `/api/cicd/integrations/:id/trigger`, `/api/cicd/runs`, GitHub Actions workflow template generation, `reasonix/agent-action@v1`, Agent-triggered employee runs, security finding exit-code mapping, CI artifacts, planned PR comments, planned auto-fix metadata, list filters, and the implementation-audit promotion of section 197 to `baseline_plus`.
- Capability negotiation API smoke checks cover `/api/capability-negotiations`, `/api/capability-negotiations/:id/resolve`, `/api/capability-negotiation-events`, missing-capability self-check, peer delegation resolution, protocol-message event linkage, list filters, and the implementation-audit promotion of section 200 to `baseline_plus`.
- Decision rollback API smoke checks cover `/api/decision-rollbacks`, `/api/decision-rollbacks/:id/apply`, decision-audit target selection, from-decision-onwards affected decisions, restart-plan metadata, non-destructive apply, listing filters, and the implementation-audit promotion of section 201 to `baseline_plus`.
- Workflow optimization API smoke checks cover `/api/workflow-optimizations`, `/api/workflow-optimizations/:id/apply`, workflow history analysis, bottleneck detection, redundancy detection, parallelization opportunities, cost optimizations, auto-apply thresholds, listing filters, and the implementation-audit promotion of section 202 to `baseline_plus`.
- Agent schedule API smoke checks cover `/api/agent-schedules`, `/api/agent-schedules/:id/evaluate`, timezone-aware weekly hours, maintenance windows, urgent overtime bypass, vacation delegation to backup Agents, persisted current status, listing filters, and the implementation-audit promotion of section 204 to `baseline_plus`.
- Task batching API smoke checks cover `/api/task-batches`, `/api/task-batches/:id/apply`, queue-window batch planning, same-Agent/type/project merge defaults, urgent/long-task exclusions, explicit batch application, source queue item cancellation with `batchedInto`, listing filters, and the implementation-audit promotion of section 205 to `baseline_plus`.
- Agent certification API smoke checks cover `/api/agent-certification-exams`, `/api/agent-certification-exams/:id/run`, `/api/agent-certification-runs`, task/rubric exam definitions, pass/fail scoring, certification badges, expiration timestamps, discovered limitations, improvement suggestions, listing filters, and the implementation-audit promotion of section 206 to `baseline_plus`.
- External monitoring API smoke checks cover `/api/external-monitoring/configs`, `/metrics`, `/health`, `/ready`, Prometheus metric names, task-duration histogram output, health/ready probe status, log export configuration, listing filters, and the implementation-audit promotion of section 207 to `baseline_plus`.
- Artifact semantic diff API smoke checks cover `/api/artifact-semantic-diffs`, structural added/modified sections, semantic high-impact security/permission explanations, risk generation, listing filters, and the implementation-audit promotion of section 208 to `baseline_plus`.
- Final acceptance API smoke checks cover `/api/acceptance-criteria`, `/api/acceptance-criteria/run`, `/api/acceptance-scenario-runs`, all 10 release scenarios, 0 failed automated checks, 5 manual-QA warning gaps, `automatedBaselineReady=true`, and the implementation-audit promotion of section 210 to `baseline_plus`.

Latest test result observed during implementation:
`29 passed test files; 376 passed, 1 skipped tests`.

## Implemented Or Partially Implemented Evidence

| Plan area | Current status | Evidence |
| --- | --- | --- |
| 1-3 Agent employee factory and Agent Profile | Baseline+ | `agent_profiles` store the employee role, model, fallback models, Skills, MCP servers, CLI profiles, software profiles, memory/autonomy/workstation/permission policies, input/output contracts, persona, behavior rules, and success criteria. `src/server/control-plane-service.ts` now builds a side-effect-free `AgentProfileCapabilityReport` that resolves selected model/Skill/MCP/CLI/software references, detects missing references, normalizes the permission matrix, summarizes input/output contracts, scores readiness, emits gaps/warnings/recommendations, and generates an employee runbook. `/api/agent-profiles/:id/capability-report`, `src/lib/api.ts` helpers, `docs/reference/agent-profile-capability-report.md`, tests, and smoke verify Agent employee readiness without launching models, tools, software, or desktop control. |
| 4 Agent employee runtime loop | Baseline+ | `src/server/employee-runtime-service.ts` runs deterministic employee phases for goal understanding, memory retrieval, planning, output-contract verification, checkpointing, context snapshotting, CLI dry-runs, multimodal IO, artifact validation, reflection, learning, and continuity. Each phase now emits `RuntimeLoopStepTrace` metadata with observation, decision, selectedAction, verification, nextStep, status, recoveryPlan, and evidence; final run output includes the full loop trace plus `nextRuntimeAction`; failure events include budget/profile recovery plans. `/api/employee-runs/:id`, `docs/reference/employee-runtime-loop.md`, tests, and smoke verify the loop without live model/tool/desktop execution. |
| 5 Memory and learning | Baseline+ | `src/server/agent-memory-service.ts` handles retrieval, privacy-aware memory visibility, memory writes, reflection, and global/filtered memory listing. `src/server/learning-service.ts` adds pending learning events and approved playbooks. `src/server/agent-memory-learning-report-service.ts` now builds a side-effect-free per-Agent report covering memory policy, owned active/expired memories, memory type/scope counts, confidence/importance, sensitive/encrypted/mistake/procedural/semantic/expiring-soon counts, sample retrieval candidates, reflection coverage, pending/approved/rejected learning events, active/draft/archived Playbooks, Playbook versions, governance review needs, readiness score, and recommendations. `/api/agent-profiles/:id/memory-learning-report`, `src/lib/api.ts` helpers, `docs/reference/agent-memory-learning-report.md`, tests, and smoke verify memory retrieval, reflection, human-reviewed learning promotion, active Playbooks, and disabled memory policy handling without auto-promoting lessons. |
| 6 Multi-agent isolation | Baseline+ | `src/server/agent-isolation-service.ts` now builds a side-effect-free isolation report for each Agent: resolved workstation mode, configured workstations, per-Agent workspace/browser/tmp paths, active computer sessions, browser/desktop/mobile/CLI/software/file/network capabilities, required resource locks, held conflicts, same-Agent held locks, v1/v2 concurrency behavior, warnings, recommendations, and a verdict of `isolated`, `needs_lock`, `conflict`, or `not_parallel_safe`. `/api/agent-profiles/:id/isolation-report`, `src/lib/api.ts` helpers, `docs/reference/multi-agent-isolation.md`, tests, and smoke verify browser/CLI parallel isolation and physical-desktop lock conflicts without controlling the real desktop. |
| 7 Resource locks | Implemented baseline | `resource_locks` schema, service, workflow integration, task-queue resource requirement integration, tests in `src/server/control-plane-service.test.ts`. |
| 8 Software CLI-ization | Partial+ | `software_profiles`, `software_commands`, `software_command_runs`, `recorded_macros`, `macro_replay_runs`; `src/server/software-adapter-service.ts`; `src/server/recorded-macro-service.ts`; software command workflow nodes in `src/server/workflow-runner-service.ts` and `src/components/agent-workflow-canvas.tsx`; `src/components/tool-control-center.tsx` can create software profiles/commands/macros, structural-test commands, dry-run software commands, dry-run macro replays, and inspect recent run history. Recorded macros can be created, dry-run replayed, indexed as capabilities, and gated behind autonomy/approval records for live replay. |
| 9 CLI Profile | Partial+ | `cli_profiles`, `cli_runs`; `src/server/cli-runner-service.ts`; `/api/cli-runs`; `src/components/tool-control-center.tsx` creates/tests CLI profiles, renders safe dry-runs with user variables, and shows CLI run history. Runtime auto-renders CLI dry-runs; execute attempts create approval requests. |
| 10 MCP abstraction | Baseline+ | `tool_connections`, `mcp_servers`, `mcp_tool_definitions`, `mcp_tool_calls`; `/api/mcp-servers` can register/test metadata; `/api/mcp-servers/:id/discover-tools`, `/api/mcp-tools`, `/api/mcp-tools/:id/run`, `/api/mcp-tool-calls`; `src/server/mcp-tool-service.ts` discovers manifest-backed tool catalogs, records dry-run calls, gates execute mode behind autonomy/approval records, and can execute approved MCP tools through the MCP JSON-RPC `initialize` -> `notifications/initialized` -> `tools/list` -> `tools/call` sequence. Local stdio execution requires `live=true`, `confirmRisk=true`, `AGENTHUB_ENABLE_REAL_MCP_PROCESS=1`, and `AGENTHUB_ALLOWED_MCP_COMMANDS`; remote HTTP-compatible endpoint execution requires `AGENTHUB_ALLOWED_MCP_ENDPOINT_HOSTS` and supports `AGENTHUB_MCP_AUTHORIZATION`, `AGENTHUB_MCP_BEARER_TOKEN`, or `AGENTHUB_MCP_API_KEY` credential headers. Live calls are bound to an approved `approvalRequestId`, validate tool/input replay, persist results in `mcp_tool_calls`, update MCP server health, and write redacted `mcp.tool.call.live` audit logs; `scripts/smoke-mcp-jsonrpc-tool-api.ts` verifies local stdio JSON-RPC, and `scripts/smoke-mcp-remote-http-tool-api.ts` verifies remote HTTP JSON-RPC with bearer authentication and endpoint host allowlisting. `src/server/mcp-runtime-service.ts` adds guarded MCP runtime lifecycle planning/status/start/stop with direct-spawn command validation, endpoint-host allowlist metadata, pid tracking for AgentHub-launched stdio processes, and audit logs; `/api/mcp-servers/:id/runtime` exposes the lifecycle API. `src/components/tool-control-center.tsx` exposes Tool Connection creation/testing, MCP server registration/testing/discovery, MCP dry-run calls, and recent call history. Customer-owned MCP side effects still require authorized servers, scoped credentials, endpoint allowlists, and onsite audit evidence. |
| 11 Model configuration | Baseline+ | `model_profiles` schema/API/UI and structural tests exist. `model_connection_tests`, `model_route_decisions`, `src/server/model-gateway-service.ts`, and `src/components/model-control-center.tsx` add dry-run/live-test records, capability checks, route preview, fallback selection, rough cost estimates, and a visible Model Control page. Capability probes now plan provider-specific JSON/tool-calling/vision requests and optional streaming handshakes for OpenAI-compatible, Anthropic, Google Gemini, and Ollama-style providers, recording stream protocol, endpoint family, vision payload presence, and handshake readiness without sending a live request. Live provider calls are opt-in and require resolvable credentials; live connection tests require `confirmExternalCall=true` plus `AGENTHUB_ENABLE_REAL_MODEL_CONNECTION=1`, while live invocation is separately gated by `AGENTHUB_ENABLE_REAL_MODEL_INVOCATION=1`, customer authorization, and the approved go-live hash. |
| 12 Network/IP outlet | Baseline+ | `network_profiles` schema/API/UI and structural tests exist. `src/server/network-egress-report-service.ts` adds a side-effect-free egress report covering direct/proxy/gateway profile counts, endpoint readiness, last health status, model-to-network assignments, Agent browser/all-traffic metadata, CLI route metadata from `NETWORK_PROFILE_ID`/`AGENTHUB_NETWORK_PROFILE_ID`, implicit direct model egress, missing route references, readiness, gaps, warnings, and recommendations. `src/server/network-egress-live-test-service.ts` adds guarded live landing-IP probes through `/api/network-profiles/:id/egress-live-test`, requiring `confirmExternalCall=true` and `AGENTHUB_ENABLE_REAL_NETWORK_EGRESS_TEST=1`; smoke verifies the proxy dispatcher path, observed IP persistence, and env-gated blocking without touching the real network. Model Gateway already applies the selected Network Profile dispatcher for live model calls. |
| 13 Canvas orchestration | Baseline+ | `workflows`, `workflow_nodes`, `workflow_edges`, `workflow_runs`, `workflow_node_runs`, and `workflow_preflights` exist; `src/server/workflow-canvas-report-service.ts` now adds a side-effect-free Canvas report that resolves Agent nodes, human approval nodes, node input mappings, node/Agent output contracts, upstream/downstream links, approval/retry flags, latest node-run visualization state, entry/terminal nodes, topological order, parallel groups, cycles, dangling edges, artifact flow, readiness, gaps, warnings, and recommendations. `/api/workflows/:id/canvas-report`, `src/lib/api.ts` helpers, `docs/reference/workflow-canvas-report.md`, tests, and smoke verify deterministic graph/contract inspection before workflow preflight or execution. |
| 14 Runtime visualization | Partial+ | Factory and Canvas show run status, events, CLI runs, computer sessions, software command runs, context snapshots, validations, locks, approvals, memory, learning. `src/server/run-event-feed-service.ts` now provides unified employee/workflow feed items and `/events?stream=1` SSE output for run visualization. `src/server/observability-service.ts` now builds Agent debug panel snapshots with current state, token/lock/memory counts, prompt/model-call references, manual action metadata, next-step simulation, and debug-package manifests. `src/app/api/computer-sessions*` records Runtime Control actions, including guarded desktop click/scroll/type/key/focus/screenshot and ADB-backed mobile list/tap/swipe/text/keyevent/screenshot attempts, into the computer-action timeline with locks, gates, runtime kill-switch state, desktop target allowlists, mobile device allowlists, input hashes, and audit metadata. |
| 15 Permissions and autonomy | Baseline+ | Profile policies are persisted; `autonomy_decisions` plus `src/server/autonomy-policy-service.ts` evaluate action risk, autonomy level, permission-policy blocks, and approval requirement. CLI/software execution paths now record autonomy decisions before approval/dry-run handling. `src/components/governance-center.tsx` exposes autonomy evaluation, approval queue review, sandbox access evaluation, security findings, and audit logs. |
| 16 SkillsMP CLI/API skill market | Baseline+ | `skills`, `skill_install_flows`, `skill_sdk_manifests`, and `skill_marketplace_publications` persist local Skill inventory, install history, developer SDK manifests, and publication metadata. `scripts/skillsmp-cli.mjs` turns SkillsMP search into a deterministic CLI, `src/server/skillsmp-cli-service.ts` wraps it for the server, and `/api/skills/skillsmp-cli` exposes it to the app. `src/server/skillsmap-integration-service.ts` adds a side-effect-free SkillsMP readiness report covering the configured marketplace URL, HTTPS/SkillsMap-like host, Skills Center CLI/API search contract, expected panels, installed/enabled/disabled Skill counts, install-flow counts by source, failed flows, SDK manifest validity, marketplace publication state, recent records, gaps, warnings, and recommendations. `/api/skills/skillsmap-report`, `src/lib/api.ts` helpers, `docs/reference/skillsmap-integration.md`, `src/components/skills-center.tsx` market search UI, tests, and API smoke verify SkillsMP discovery plus local Skill management without iframe embedding or unapproved installs. |
| 17 Mobile connection | Baseline+ | Mobile Companion now exposes `getMobileCompanionReport` in `src/server/mobile-service.ts`, reporting companion mode, token/auth readiness, CORS origins, endpoint contract, companion capabilities, current snapshot counts, recent upload handoffs, gaps, warnings, recommendations, and a guarded device-automation boundary. Android ADB and mobile click/input automation are reported as `guarded_available` with runtime action names, required env gates, `AGENTHUB_ALLOWED_MOBILE_DEVICE_IDS` / `AGENTHUB_ALLOWED_MOBILE_APP_PACKAGES` allowlist status, approval/go-live/live-pilot/resource-lock/audit safety gates, and evidence. iOS Shortcuts, full Appium execution, and screen mirroring remain explicitly `reserved_not_enabled`. Runtime Control includes guarded ADB-backed `list_devices`, `mobile_tap`, `mobile_swipe`, `mobile_text`, `mobile_keyevent`, and `mobile_screenshot` actions; write/control actions still require env gates, resource locks, approvals, go-live checks, device/app allowlists, and target-app foreground checks before a live phone can be touched. `AGENTHUB_ADB_PATH` and `AGENTHUB_ADB_ARGS_PREFIX_JSON` let customer machines use an explicit adb.exe path or signed wrapper, are included in the go-live environment fingerprint, and are covered by `scripts/smoke-runtime-adb-execution-api.ts`, which proves production mobile discovery and Runtime Control `list_devices` execute the same configured command path without controlling a real phone. `getMobileSnapshot` includes recent phone-provided upload handoffs, `/api/mobile/uploads` registers phone images/screenshots/audio/text/structured files as Agent-visible `multimodal_inputs`, and `/api/mobile/companion-report` verifies readiness without requiring a real phone in local smoke. |
| 18 Database tables | Baseline+ | `src/server/database-coverage-report-service.ts` verifies all 26 Section 18 persistence requirements against `src/db/schema.ts`, `src/db/bootstrap.ts`, and the live SQLite table list. It reports 24 physical table requirements and 2 embedded JSON policy requirements: `agent_permissions` is persisted through `agent_profiles.permission_policy`, and `agent_memory_policies` is persisted through `agent_profiles.memory_policy`. The report checks schema exports, bootstrap DDL, current table presence, categories, gaps, and recommendations for model/network/Agent/workstation/memory/learning/tool/MCP/CLI/software/macro/workflow/resource-lock/approval/artifact-validation persistence. `/api/database/coverage-report`, `src/lib/api.ts` helpers, `docs/reference/database-coverage-report.md`, tests, and API smoke verify the database foundation without destructive migrations. |
| 19 Backend services | Baseline+ | `src/server/backend-service-coverage-report-service.ts` verifies the 17 Section 19 service requirements as explicit dedicated or composite backend services: `ModelProfileService`, `NetworkProfileService`, `AgentProfileService`, `AgentEmployeeRuntime`, `AgentMemoryService`, `LearningService`, `CanvasWorkflowService`, `WorkflowRunner`, `ToolConnectionService`, `McpService`, `CliRunner`, `SoftwareAdapterService`, `ComputerSessionManager`, `ResourceLockService`, `ArtifactService`, `VerificationService`, and `ApprovalService`. It reports 11 dedicated services, 6 composite services, and all 5 critical services covered, with file existence, expected exports, API evidence, gaps, and recommendations. `/api/backend-services/coverage-report`, `src/lib/api.ts` helpers, `docs/reference/backend-service-coverage-report.md`, tests, and API smoke verify the backend service layer without starting live models, MCP servers, browser sessions, or desktop automation. |
| 20 API design | Baseline+ | `src/server/api-design-coverage-report-service.ts` verifies the 35 Section 20 control-plane endpoint contracts for model profiles, network profiles, Agent profiles, Skills, Tool Connections, CLI profiles, software profiles/commands, workflows, workflow runs, Agent-run memory/reflection, and approvals. It checks App Router route-file existence and exported HTTP method handlers, reporting 32 exact endpoints, 3 compatible v1 employee-run control endpoints for workflow pause/resume/cancel, 0 missing endpoints, per-category coverage, warnings, and recommendations. `/api/api-design/coverage-report`, `src/lib/api.ts` helpers, `docs/reference/api-design-coverage-report.md`, tests, and API smoke make the API design contract machine-checkable. |
| 21 Frontend pages | Baseline+ | `src/server/frontend-page-coverage-report-service.ts` verifies the eight Section 21 frontend workbench surfaces: Agent Factory, Model Control, Tool Control, Software CLI-ization, Skills Center, Agent Canvas, Run Monitoring, and Memory/Learning. It checks sidebar import/mode/label/render markers plus component-level capability markers, reporting 8 covered surfaces, 6 dedicated workbenches, 2 composite workbenches, 0 missing pages, and composite warnings for Software CLI-ization inside Tool Control and Run Monitoring across Factory/Canvas/Observability. `/api/frontend-pages/coverage-report`, `src/lib/api.ts` helpers, `docs/reference/frontend-page-coverage-report.md`, tests, and API smoke make page coverage machine-checkable. |
| 22 Phase plan | Baseline+ | `src/server/phase-plan-coverage-report-service.ts`; `/api/phase-plan/coverage-report`; `docs/reference/phase-plan-coverage-report.md`; `scripts/smoke-phase-plan-coverage-api.ts`; `src/lib/api.ts` helpers. Tests and API smoke verify all seven documented phases are `baseline_ready`: Phase 1 control plane foundation, Phase 2 Agent employee runtime, Phase 3 Canvas orchestration, Phase 4 memory and learning, Phase 5 computer/browser operation, Phase 6 software CLI-ization, and Phase 7 guarded virtual workstations. Phase 7 now checks `virtual_desktop`, `vm`, and `remote_session` schema support plus Runtime Control validation/launch/release, RDP/VNC/browser/Hyper-V/VirtualBox/VMware launch plans, workstation target allowlists, provider discovery, and stale lease recovery without claiming automatic cloud VM provisioning. |
| 23 Testing | Baseline+ | src/server/test-plan-coverage-report-service.ts; /api/test-plan/coverage-report; docs/reference/test-plan-coverage-report.md; scripts/smoke-test-plan-coverage-api.ts; src/lib/api.ts helpers. Tests and API smoke verify all 18 required Section 23 test cases: model connection success/failure, proxy/IP outlet tests, Agent permission interception, CLI Profile execution, MCP tool invocation, Skills install/enable, Agent runtime loop, output artifact validation, multi-Agent parallel boundaries, resource lock conflicts, browser session isolation, file write isolation, memory write/retrieval, learning review, software macro record/replay, Canvas node status updates, human approval pause/resume, and failure recovery/retry. |
| 24 Product effects | Baseline+ | src/server/product-effects-coverage-report-service.ts; /api/product-effects/coverage-report; docs/reference/product-effects-coverage-report.md; scripts/smoke-product-effects-coverage-api.ts; src/lib/api.ts helpers. Tests and API smoke verify 13 promised outcomes: Agent employee factory, per-Agent independent configuration, self-planning runtime, memory/learning, CLI orchestration, guarded computer/browser operation, Canvas team workflow, progress visibility, verifiable artifacts, multi-Agent parallel boundaries, workstation/resource-lock safety, software CLI-ization, and guarded local AI employee OS foundation. Runtime Control now records desktop, ADB mobile, and VM/RDP/VNC workstation validation plus launch/release lifecycle state, marking successfully launched workstations busy and release actions idle; production workstation recovery adds `/api/production-integrations/workstations/recovery` to dry-run and safely recover stale busy workstation leases when no active session or held lock remains. The report now expects 0 purely reserved promised effects while still separating guarded effects so the product does not overclaim unrestricted live desktop/mobile/account/payment/destructive-file/external-VM autonomy. |
| 25 Default assumptions | Followed so far | Local-first, Windows-friendly paths, true desktop control held back behind dry-run/approval boundaries. |
| 26 Security and secret management | Baseline+ | `secret_vault`, `credential_scopes`, `sandbox_policies`, `audit_logs`, `security_findings`; `src/server/security-service.ts`; `/api/security/*`; employee runtime writes security audit logs for queue/complete/fail. `src/components/governance-center.tsx` exposes env-ref secret registration, credential scoping, sandbox policy creation/evaluation, prompt-injection scan, security finding creation, and audit log review without asking users to paste raw secret values. |
| 27 Cost and budget control | Baseline+ | `budget_policies`, `budget_evaluations`, and existing `budget_events`; `src/server/budget-control-service.ts`; `/api/budget-control/policies/seed`, `/api/budget-control/policies`, `/api/budget-control/evaluate`, `/api/budget-control/evaluations`, `/api/budget-control/usage-report`; `src/lib/api.ts` helpers; `docs/reference/budget-control.md`. Tests and API smoke cover per-task, per-Agent-per-day, per-project-month, and global-month policies; `token_count` and `usd_amount` limits; hard caps; notify thresholds; projected usage from historical runs plus observed and estimated spend; model-routing hints; model/tool/CLI cost breakdowns; stop-task and notify-user decisions; list filters; and usage reports with CSV export. |
| 28 Observability, metrics, alerts, debug replay, health scoring | Baseline+ | `metric_points`, `alert_rules`, `alert_events`, `debug_replay_snapshots`, `agent_health_scores`; `src/server/observability-service.ts`; `/api/observability/*`; `src/components/observability-center.tsx`; employee runtime records duration/cost/failure metrics; tests verify alerts, notifications, debug replay, and health score. The UI can record metric points, create alert rules, filter alert events, capture employee-run debug replays, compute Agent health, score Agent reputation, create/read notifications, and manage notification preferences. |
| 29 Task scheduling and queues | Baseline+ | `task_queues`, `task_queue_items`, `task_schedules`; `src/server/scheduler-service.ts`; `src/components/task-scheduler-center.tsx`; `/api/task-queues`, `/api/task-queue-items`, `/api/task-queues/:id/tick`, `/api/task-schedules`, `/api/task-schedules/run-due`, `/api/continuation-plans/enqueue-due`; queue processor can run due `employee_run` tasks and `continuation_plan` tasks through the deterministic employee runtime, update continuation-plan status, scan due continuation plans into queues once, execute scan-and-process ticks, run persisted recurring schedules, acquire/release task resource locks, fail cleanly on resource conflicts, record audit logs, and expose manual controls in the Scheduler UI. |
| 30 Crash recovery and state persistence | Baseline | Existing `runtime_checkpoints` now emit `recovery_events`; `idempotency_records`; `src/server/recovery-service.ts`; `/api/recovery-events`, `/api/idempotency-records`, `/api/employee-runs/:id/recovery-summary`; employee run snapshots include recovery events. |
| 31 Prompt and context-window management | Baseline+ | Extended `prompt_templates` with engine/template/variables/conditional blocks, extended `prompt_template_versions` with content/A-B metadata/deploy lifecycle, and added `context_compressor_policies` plus `context_compression_plans`. `src/server/prompt-context-service.ts` renders Prompt templates from Agent/task/memory/runtime/env/static sources, evaluates conditional blocks, returns Prompt A/B metadata and token estimates, seeds default 80-percent compressor policies, allocates model-call token budgets for system prompt/current plan/relevant memories/recent summaries/tool definitions/safety margin/full recent steps, and records preserved/compressed/omitted context sections. `/api/prompt-templates/:id/render`, `/api/context-compressors/policies`, `/api/context-compressors/policies/seed`, `/api/context-compressors/plan`, `/api/context-compressors/plans`, `src/lib/api.ts` helpers, `docs/reference/prompt-context-management.md`, tests, and API smoke cover the Section 31 flow. |
| 32 Human collaboration enhancement | Baseline+ | `human_approval_policies`, `plan_approval_results`, and `takeover_sessions`; `src/server/human-collaboration-service.ts`; `/api/human-collaboration/approval-policies`, `/api/human-collaboration/approval-policies/:id/evaluate`, `/api/human-collaboration/plan-approvals`, `/api/human-collaboration/takeovers`, `/api/human-collaboration/takeovers/:id/actions`, and `/api/human-collaboration/takeovers/:id/complete`; `src/lib/api.ts` helpers; `docs/reference/human-collaboration.md`. Tests and API smoke cover approval timeouts, timeout actions, batching recommendations, auto-approval conditions with per-run caps, escalation chains, partial step approvals with approved/rejected/modified/skipped decisions, linked approval request resolution, takeover start/action/complete records, and completed takeover listing. |
| 33 Agent communication and collaboration | Baseline+ | `inter_agent_messages`, `blackboard_entries`, `conflict_resolutions`; `src/server/collaboration-service.ts`; `/api/collaboration/messages`, `/api/collaboration/blackboard`, `/api/collaboration/conflicts`; `src/components/collaboration-center.tsx`; tests cover handoff messages, versioned blackboard entries, and conflict resolution. The UI can send Agent messages, write versioned shared state, open conflict records, resolve conflicts, and inspect current collaboration state. |
| 34/198/199 Config versioning, GitOps export, and reproducibility | Baseline+ | `config_versions`, `config_exports`, `config_impact_analyses`; `src/server/config-version-service.ts`; `/api/config-versions`, `/api/config-versions/:id/apply`, `/api/config-exports`, `/api/config-impact-analyses`; `src/components/config-ops-center.tsx`; tests cover hash-based Agent/workflow config snapshots, GitOps bundle export, config restore with rollback snapshot capture, and impact analysis for workflows referencing an Agent profile. The UI can select known config entities, capture snapshots, restore a selected version after capturing rollback state, export bundles, run impact analysis, and inspect snapshot/export/impact payloads. Real Git repository sync remains pending. |
| 35 Plugin and extension framework | Baseline+ | `plugin_packages`, `plugin_lifecycle_events`; `src/server/plugin-framework-service.ts`; `/api/plugins`, `/api/plugins/:id/enable`, `/api/plugins/:id/disable`, `/api/plugins/:id/uninstall`, `/api/plugins/:id/upgrade`, `/api/plugins/:id/health-check`, `/api/plugins/:id/compatibility`, `/api/plugins/:id/events`, `/api/system/plugins`, `/api/system/plugins/install`; `src/lib/api.ts` helpers; `docs/reference/plugin-framework.md`. Tests and API smoke cover all documented extension points, plugin install/enable/disable/uninstall/upgrade lifecycle, capability definitions, marketplace metadata, compatibility reports, deterministic security scan warnings/blocks, health checks, lifecycle event history, listing filters, and audit logs without executing third-party plugin code. |
| 36 Data lifecycle and compliance | Baseline+ | `retention_policies`, `storage_quota_snapshots`, `pii_markers`, `data_export_manifests`; `src/server/data-lifecycle-service.ts`; `/api/data-lifecycle/*`; `src/components/governance-center.tsx`; tests cover retention policy creation and dry-run evaluation, storage quota snapshots, regex PII marker detection/review, and portable export manifests that exclude secret values. Destructive retention actions, true archive packaging, and full legal compliance workflows remain pending. |
| 37 Multi-user and team collaboration | Baseline+ | `team_users`, `teams`, `team_memberships`, `team_resource_shares`, `team_approval_policies`, `team_approval_decisions`; `src/server/team-collaboration-service.ts`; `/api/team-users`, `/api/teams`, `/api/teams/:id/members`, `/api/team-permissions/evaluate`, `/api/team-resource-shares`, `/api/team-approval-policies`, `/api/team-approval-policies/:id/decisions`, `/api/team-approval-policies/:id/evaluate`; `src/lib/api.ts` helpers; `docs/reference/team-collaboration.md`. Tests and API smoke cover admin/operator/viewer/custom roles, documented and custom permission keys, global/project scope checks, team-shared/project-shared/private resources, user-isolated model secret handling, specific-user/any-approver/all-must-approve/one-of-many approvals, approval decision permission checks, approval resolution, listing filters, and audit logs. |
| 39 System bootstrap and meta monitoring | Baseline+ | `system_bootstrap_checks`; `src/server/system-bootstrap-service.ts`; `/api/system-bootstrap/checks/run`; `/api/system-bootstrap/checks`; `src/lib/api.ts` helpers; `docs/reference/system-bootstrap.md`; existing `meta_agent_*` digest/recommendation support. Tests and API smoke cover database, queue, model provider, MCP server, disk-space reservation, memory, running-Agent, pending-approval, API latency, WebSocket, event-throughput, slow-query, checkpoint-latency, and Ops/Meta Agent availability checks with threshold warnings, run grouping, component/status filters, and local-only self-healing recommendations. |
| 40 Structured knowledge graph | Baseline+ | `knowledge_graph_nodes.embedding`; expanded node types for person/project/software/concept/file/error/solution/customer; expanded relation types for uses/depends_on/solves/causes/belongs_to/prefers/avoids/alternative_to; `src/server/knowledge-graph-service.ts`; `/api/knowledge-graph`; `/api/knowledge-graph/query`; `src/lib/api.ts` helpers; `docs/reference/knowledge-graph.md`. Tests and API smoke cover deterministic local embeddings, memory extraction into customer preferences and error-solution paths, software profile/command graph links, software command successful-run evidence, relation filters, and scenario-specific semantic graph queries without external model calls. |
| 41 Simulation, backtesting, and Golden Set gates | Baseline+ | `simulation_runs`, `golden_task_sets`, `backtest_runs`; `src/server/simulation-backtest-service.ts`; `/api/simulations`, `/api/simulations/:id/review`, `/api/golden-task-sets`, `/api/backtests`; `src/lib/api.ts` helpers; `docs/reference/simulation-backtesting.md`. Tests and API smoke cover simulated-only Agent dry-runs, fixture/user-played tool results, planned steps that do not mutate real resources, approve/reject/adjust review, reusable Golden Sets with explicit success criteria, historical and golden backtests, baseline/candidate scoring, success-rate deltas, and passed/failed CI gate outcomes. |
| 42 Error taxonomy and self-recovery strategy | Baseline+ | `error_classifications`, `recovery_strategy_attempts`, `recovery_strategy_stats`; `src/server/error-recovery-strategy-service.ts`; `/api/error-recovery/classify`, `/api/error-recovery/recommend`, `/api/error-recovery/classifications`, `/api/error-recovery/attempts`, `/api/error-recovery/strategy-stats`; `src/lib/api.ts` helpers; `docs/reference/error-recovery-strategies.md`. Tests and API smoke cover the documented error taxonomy, severity assignment, ranked recovery strategies, retry failure vs alternate-strategy success-rate reranking, permission errors pausing for user help, model errors preferring fallback models, listings, and local record-only stats updates. |
| 43 Agent identity and persona | Baseline+ | `agent_profiles.persona`; `src/server/control-plane-service.ts` normalizes avatar/tone/language/communication style/self-reference/personality traits; Agent Profile create/patch validators accept structured Persona; `src/components/employee-agent-factory.tsx` exposes Persona controls; `src/server/employee-runtime-service.ts` emits Persona and `personaDecisionStyle`; `src/server/prompt-context-service.ts` includes Persona in visible runtime context; `docs/reference/agent-persona.md`. Tests verify create/update persistence, runtime event payloads, context snapshot visibility, and decision-style projection. |
| 44 Agent templates and marketplace | Baseline+ | `agent_template_packages`, `agent_template_installs`; `src/server/agent-template-marketplace-service.ts`; `/api/agent-templates`, `/api/agent-templates/seed`, `/api/agent-templates/:id/publish`, `/api/agent-templates/:id/install`, `/api/agent-template-installs`; `src/lib/api.ts` helpers; `docs/reference/agent-template-marketplace.md`. Tests and API smoke cover the 20 default templates for development/design/operations/office/project roles, custom template publishing for Agent Profile/Workflow/Skill package/Software Command/Macro package types, real Agent Profile creation from templates, record-only v1 installs for non-Agent template types, install-count updates, variable substitution, install history, filters, and audit logs. |
| 45 Offline and degradation strategy | Baseline+ | `degradation_policies`, `degradation_events`; `src/server/degradation-service.ts`; `/api/degradation/policies`, `/api/degradation/evaluate`, `/api/degradation/events`; `src/components/governance-center.tsx` exposes offline fallback policy creation, model/MCP/network/API/browser/queue trigger evaluation, and event review. Defaults match the plan: model fallback, MCP fallback, direct network route, external API pending retry, browser pause-until-online, and queue state preservation. |
| 46 Technical architecture supplement | Baseline+ | `technical_architecture_evaluations`; `src/server/technical-architecture-service.ts`; `/api/technical-architecture/manifest`, `/api/technical-architecture/evaluate`, `/api/technical-architecture/evaluations`; `src/lib/api.ts` helpers; `docs/reference/technical-architecture.md`. Tests and API smoke cover the selected Electron+Node desktop architecture, React/TypeScript/Zustand/Tailwind frontend, custom Canvas/CodeMirror implementation warning, SQLite WAL, EventEmitter/run-event feed, CLI child-process isolation, browser/computer session managers, AES-GCM secret vault, Section 46 supplemental table mappings, and config -> Canvas -> runtime -> verification -> learning data flow. |
| 47 Test strategy supplement | Baseline+ | `test_strategy_items`; `src/server/test-strategy-service.ts`; `/api/test-strategy/seed`, `/api/test-strategy/items`, `/api/test-strategy/evaluate`; `src/lib/api.ts` helpers; `docs/reference/test-strategy.md`. Tests and API smoke cover the Section 47 pyramid (`unit`, `integration`, `e2e`), AgentEmployeeRuntime/ResourceLockService/MemoryService integration checklist, deterministic Mock model output/error-injection/call-recording capabilities, record-only chaos cases for child-process kill/network disconnect/model error/disk-full scenarios, and explicit protection against live OS/network/disk chaos mutation in v1. |
| 48 Documentation and help system | Baseline+ | `help_center_surfaces`, `help_center_items`, `help_onboarding_flows`; `src/server/help-center-service.ts`; `/api/help-center/seed`, `/api/help-center/surfaces`, `/api/help-center/items`, `/api/help-center/onboarding-flows`; `src/lib/api.ts` helpers; `docs/reference/help-center.md`. Tests and API smoke cover 10 default configuration surfaces, `?` question buttons, hover tooltip records, example values, troubleshooting error-document links, first-Agent -> first-task -> first-artifact onboarding, custom help surface/item/flow creation, listing filters, idempotent seeding, and audit logs. |
| 49 Internationalization (i18n) | Baseline+ | `i18n_contract_checks` plus existing `localization_settings`, `localization_resources`, and `agent_localization_policies`; `src/server/localization-service.ts`; `/api/localization/contract/seed`, `/api/localization/contract/checks`, `/api/localization/contract/evaluate`, `/api/localization/resolve`; `src/lib/api.ts` helpers; `docs/reference/localization.md`. Tests and API smoke cover UI i18n-key checks, Agent system prompt language following `personaLanguage`, locale-aware date/time/number formatting, multilingual error resources, multilingual docs resources, fallback translation, and all five Section 49 checks passing. |
| 50 Accessibility | Baseline+ | `accessibility_profiles`; `src/server/accessibility-profile-service.ts`; `/api/accessibility/profiles/seed`, `/api/accessibility/profiles`, `/api/accessibility/profiles/:id/evaluate`; `src/lib/api.ts` helpers; `docs/reference/accessibility.md`. Tests and API smoke cover keyboard navigation through shortcut registry linkage, screen-reader support metadata, high-contrast theme linkage, font-size adjustment through `fontScale`, light/dark/system color-scheme support, default and custom profiles, five passing check results, and docs existence. |
| 51 Future architecture evolution | Baseline+ | `architecture_evolution_reservations`; `src/server/architecture-evolution-service.ts`; `/api/future-architecture/reservations/seed`, `/api/future-architecture/reservations`, `/api/future-architecture/reservations/evaluate`; `src/lib/api.ts` helpers; `docs/reference/future-architecture.md`. Tests and API smoke cover `IEventBus`, `IStorage`, `ILockService`, `IRuntimeWorker`, `IDeploymentTarget`, and `IMobileAgentSurface` reservations for Redis/NATS event bus, PostgreSQL/LiteFS/shared storage, Redis/etcd distributed locks, optional cloud workers, SaaS/private deployment, and future mobile voice/image/AR/app-control surfaces while preserving v1 local-first scope. |
| 89 OS-level interference handling | Baseline+ | `os_interference_policies`, `os_interference_events`; `src/server/os-interference-service.ts`; `/api/os-interference/policies/seed`, `/api/os-interference/policies`, `/api/os-interference/evaluate`, `/api/os-interference/events`; `src/lib/api.ts` helpers; `docs/reference/os-interference.md`. Tests and API smoke cover system popups, application dialogs, screen/session states, UAC notify-user handling, native-file-picker CLI/API bypass, screen-lock pause-all, RDP-disconnect pause-UI, low-battery pause, no-interference continue, prevention checklist guidance, and explicit record-only safety boundaries against clicking UAC or mutating live desktop/OS/network state. |
| 90 File-system boundary cases | Baseline+ | `file_system_boundary_policies`, `file_system_boundary_evaluations`; `src/server/file-system-boundary-service.ts`; `/api/file-boundaries/policies/seed`, `/api/file-boundaries/policies`, `/api/file-boundaries/evaluate`, `/api/file-boundaries/evaluations`; `src/lib/api.ts` helpers; `docs/reference/file-system-boundaries.md`. Tests and API smoke cover encoding/BOM/line-ending drift, Windows MAX_PATH pressure, file-lock handling, medium/large/huge-file actions, binary metadata-only routing, unsafe filename normalization, shallow workspace guidance, and record-only safety without opening, locking, modifying, deleting, or executing user files. |
| 91 Browser automation traps | Baseline+ | `browser_automation_trap_policies`, `browser_automation_trap_evaluations`; `src/server/browser-automation-trap-service.ts`; `/api/browser-automation-traps/policies/seed`, `/api/browser-automation-traps/policies`, `/api/browser-automation-traps/evaluate`, `/api/browser-automation-traps/evaluations`; `src/lib/api.ts` helpers; `docs/reference/browser-automation-traps.md`. Tests and API smoke cover ad blocker/password manager/translator/writing assistant interference, clean-profile and extension-disable guidance, zoom 100%, fixed viewport, DPI/color/GPU drift, selector-first locators, SSIM comparison, Cloudflare/reCAPTCHA/hCaptcha pause-and-notify, user-approved session reuse, and explicit no-CAPTCHA-bypass safety boundaries. |
| 92 Enterprise network adaptation | Baseline+ | `enterprise_network_policies`, `enterprise_network_evaluations`; `src/server/enterprise-network-service.ts`; `/api/enterprise-network/policies/seed`, `/api/enterprise-network/policies`, `/api/enterprise-network/evaluate`, `/api/enterprise-network/evaluations`; `src/lib/api.ts` helpers; `docs/reference/enterprise-network.md`. Tests and API smoke cover HTTP/HTTPS/SOCKS5/PAC/system proxy modes, none/basic/NTLM/Kerberos/negotiate auth, Secret Vault password references, Node proxy agents, browser proxy injection, Python `requests-ntlm`, noProxy loopback entries, self-signed/corporate CA/SSL-inspection/missing-CA-bundle issues, `NODE_EXTRA_CA_CERTS`, `SSL_CERT_FILE`, `REQUESTS_CA_BUNDLE`, and record-only safety without changing system proxy settings, installing certificates, disabling TLS verification, or making network calls. |
| 93 Agent output control and consistency | Baseline+ | `output_consistency_policies`, `output_consistency_evaluations`; `src/server/output-consistency-service.ts`; `/api/output-consistency/policies/seed`, `/api/output-consistency/policies`, `/api/output-consistency/evaluate`, `/api/output-consistency/evaluations`; `src/lib/api.ts` helpers; `docs/reference/output-consistency.md`. Tests and API smoke cover output-language enforcement, mixed-language detection, English code-comment policy, formatter mapping for `.ts`, `.tsx`, `.js`, `.jsx`, `.css`, `.md`, `.py`, and `.go`, missing/failed formatter handling, pass/warning/rejected statuses, and record-only safety without rewriting artifacts or running formatters directly. |
| 94 System resource contention and adaptation | Baseline+ | `resource_governor_policies`, `resource_governor_evaluations`; `src/server/resource-governor-service.ts`; `/api/resource-governor/policies/seed`, `/api/resource-governor/policies`, `/api/resource-governor/evaluate`, `/api/resource-governor/evaluations`; `src/lib/api.ts` helpers; `docs/reference/resource-governor.md`. Tests and API smoke cover total/per-Agent CPU, memory, GPU VRAM, disk I/O, network throughput, foreground-agent preservation, background throttling, battery-mode max concurrency, low-battery low-priority pause, critical-battery force checkpoint, local model disablement, cheaper-model fallback, browser slowdown, CPU/GPU thermal pressure, tray-status guidance, and record-only safety without changing OS power settings, killing processes, throttling hardware, or controlling devices. |
| 95 Global operating-system integration details | Baseline+ | `global_os_integration_policies`, `global_os_integration_evaluations`; `src/server/global-os-integration-service.ts`; `/api/global-os-integration/policies/seed`, `/api/global-os-integration/policies`, `/api/global-os-integration/evaluate`, `/api/global-os-integration/evaluations`; `src/lib/api.ts` helpers; `docs/reference/global-os-integration.md`. Tests and API smoke cover virtual clipboard, direct input dispatch, system clipboard backup/restore fallback, headless/browser background preference, recent-user-input foreground delay, foreground approval, native file-picker file-input injection, print-to-PDF API replacement, CSS color injection, unknown native dialog escalation, and record-only safety without reading/writing the real clipboard, changing focus, sending keystrokes, or controlling native OS dialogs. |
| 96 Progressive rollout and feature flags | Baseline+ | `feature_flags`, `feature_flag_evaluations`; `src/server/feature-flag-service.ts`; `/api/feature-flags`, `/api/feature-flags/:id/evaluate`, `/api/feature-flag-evaluations`; `src/components/governance-center.tsx` exposes flag creation, dependency/conflict configuration, rollout percent, target users, remote disable, and evaluation history. Tests cover beta targeting, dependency checks, deterministic rollout buckets, disabled non-target users, and remote rollback blocking. |
| 97 Telemetry and usage analytics | Baseline+ | `telemetry_policies`, `telemetry_events`, `telemetry_export_manifests`; `src/server/telemetry-policy-service.ts`; `/api/telemetry/policies/seed`, `/api/telemetry/policies`, `/api/telemetry/events/evaluate`, `/api/telemetry/events`, `/api/telemetry/export`, `/api/telemetry/exports`; `src/lib/api.ts` helpers; `docs/reference/telemetry-policy.md`. Tests and API smoke cover default off/explicit first-start consent, no default opt-in, minimal/usage/performance/full/off level evaluation, aggregate-only usage counts, full-level sensitive redaction, sensitive-only blocking, sanitized local event records, user-exportable manifests, and record-only safety without uploading telemetry or collecting API keys, user files, Agent outputs, memory content, browser screenshots, clipboard data, or credentials. |
| 99 Model invocation optimization | Baseline+ | `model_invocation_optimization_policies`, `model_response_cache_entries`, `model_warmup_sessions`, `model_invocation_optimization_events`; `src/server/model-invocation-optimization-service.ts`; `/api/model-optimization/policies/seed`, `/api/model-optimization/policies`, `/api/model-optimization/cache/evaluate`, `/api/model-optimization/cache-entries`, `/api/model-optimization/parameters/resolve`, `/api/model-optimization/warmups`, `/api/model-optimization/warmups/:id/complete`, `/api/model-optimization/events`; `src/lib/api.ts` helpers; `docs/reference/model-invocation-optimization.md`. Tests and API smoke cover exact/semantic/none cache policies, 60s exact TTL, 300s semantic TTL, 0.97 semantic threshold, no-cache task planning/creative generation/safety-critical bypasses, hit/miss/saved-cost stats, per-task model parameters, Agent-specific overrides, and record-only warmup/connection-pool plans without live provider calls. |
| 100 Runtime micro-operations | Baseline+ | `runtime_micro_operation_policies`, `runtime_micro_operation_decisions`, `scheduled_actions`, `agent_inbox_items`; `src/server/runtime-micro-operation-service.ts`; `/api/runtime-micro-operations/policies/seed`, `/api/runtime-micro-operations/policies`, `/api/runtime-micro-operations/timeouts/evaluate`, `/api/runtime-micro-operations/busy/evaluate`, `/api/runtime-micro-operations/decisions`, `/api/runtime-micro-operations/scheduled-actions`, `/api/runtime-micro-operations/scheduled-actions/run-due`, `/api/runtime-micro-operations/inbox`, `/api/runtime-micro-operations/inbox/process-next`; `src/lib/api.ts` helpers; `docs/reference/runtime-micro-operations.md`. Tests and API smoke cover waiting-approval/idle/stuck timeouts, busy-task queue/preempt/delegate decisions, delayed scheduled actions with busy-Agent queueing, and priority Agent inbox processing. |
| 101 Workflow advanced operations | Baseline+ | `workflow_partial_rerun_plans`, `task_merge_suggestions`, `workflow_template_instantiations`; `src/server/workflow-advanced-operation-service.ts`; `/api/workflow-advanced-operations/partial-reruns`, `/api/workflow-advanced-operations/partial-reruns/:id/apply`, `/api/workflow-advanced-operations/task-merge-suggestions`, `/api/workflow-advanced-operations/task-merge-suggestions/:id/decide`, `/api/workflow-advanced-operations/task-merge-suggestions/:id/apply`, `/api/workflow-advanced-operations/template-instantiations`; `src/lib/api.ts` helpers; `docs/reference/workflow-advanced-operations.md`. Tests and API smoke cover partial rerun from C while A/B remain cached, C/D/E queue reset, task merge approve/reject/apply flow, saved-call benefits, template parameter validation, `{{param}}` rendering, and new draft workflow creation. |
| 102 Data maintenance and storage optimization | Baseline+ | `data_maintenance_policies`, `data_maintenance_runs`; `src/server/data-maintenance-service.ts`; `/api/data-maintenance/policies/seed`, `/api/data-maintenance/policies`, `/api/data-maintenance/runs`; `src/lib/api.ts` helpers; `docs/reference/data-maintenance.md`. Tests and API smoke cover default and custom policies, event-log over-limit warnings, record-only summarize/delete/compress planning, SQLite weekly maintenance operations with backup and integrity check, completed-run temp cleanup candidates, artifact/checkpoint/long-term-work preservation, browser cache cleanup policy, cookie retention, 500MB profile warnings, inactive-profile archive candidates, and audit promotion without deleting user files. |
| 103 Concurrent editing and optimistic locks | Baseline+ | `optimistic_locks`, `edit_conflicts`; `src/server/optimistic-lock-service.ts`; `/api/optimistic-locks`, `/api/optimistic-locks/session`, `/api/optimistic-locks/commit`, `/api/edit-conflicts`, `/api/edit-conflicts/:id/resolve`; `src/components/config-ops-center.tsx` exposes Start Edit, Commit Edit, conflict list, and latest conflict resolution. Tests cover matching-version commit, stale-version conflict creation with conflicting fields, and merge resolution. |
| 104 Agent probation and risk tiering | Baseline+ | `agent_probation_records`, `agent_environment_promotions`; `src/server/agent-probation-service.ts`; `/api/agent-probation`, `/api/agent-probation/evaluate`, `/api/agent-probation/promotions`, `/api/agent-probation/promotions/:id/decide`, `/api/agent-probation/promotions/:id/apply`; `src/lib/api.ts` helpers; `docs/reference/agent-probation.md`. Tests and API smoke cover automatic probation records for new Agents, staging default environment, high-risk probation tier, autonomy/write/network/token/concurrency/output restrictions, 10-task/>80% graduation checks, auto graduation, approval-request creation for production promotion, approval/rejection persistence, A/B comparison evidence, and apply-only-after-approved promotion to production. |
| 105 Agent clone, compare, and what-if editing | Baseline+ | `agent_clone_records`, `agent_comparison_reports`, `agent_what_if_analyses`; `src/server/agent-experiment-service.ts`; `/api/agent-profiles/:id/clone`, `/api/agent-comparisons`, `/api/agent-what-if`; `src/lib/api.ts` helpers; `docs/reference/agent-experiments.md`. Tests and API smoke cover right-click-style Agent cloning with model swap notes, shared/independent-snapshot Skill modes, semantic-only memory copy without working/episodic memory, permission-policy copying, side-by-side model/skills/success-rate/cost/steps comparison over repeated tasks, and what-if estimates for cost, latency, quality, context-window shrink, memory compatibility, and affected workflows. |
| 107 Knowledge decay visualization | Baseline+ | `memory_decay_snapshots`; `src/server/memory-decay-service.ts`; `/api/memory-decay-snapshots`, `/api/memory-decay-snapshots/:id`, `/api/memory-decay-snapshots/:id/actions`; `src/lib/api.ts` helpers; `src/components/memory-learning-center.tsx`; `docs/reference/memory-decay-visualization.md`. Tests and API/browser smoke cover importance-over-time point generation, pinned/fresh/decaying/expiring-soon/expired states, solid vs dashed lines, circle vs square markers, cleanup summaries, Chinese detail text, explicit-confirmation delete planning, pin/update actions, and Memory Center SVG rendering with clickable memory points. |
| 108 Context pollution and memory poisoning protection | Baseline+ | `memory_integrity_policies`, `memory_integrity_evaluations`; `src/server/memory-integrity-service.ts`; `/api/memory-integrity/policies/seed`, `/api/memory-integrity/policies`, `/api/memory-integrity/evaluate`, `/api/memory-integrity/scan`, `/api/memory-integrity/evaluations`; `src/lib/api.ts` helpers; `docs/reference/memory-integrity.md`. Tests and API smoke cover source confidence mapping, external web/file/task-inferred confidence reduction, dangerous pattern detection for hardcoded secrets, SSL bypass, disabled security, committed secrets, and auth bypass instructions, block/flag/warn decisions, contradiction detection against high-confidence trusted memories, periodic scan summaries, and record-only review actions without deleting or rewriting memories. |
| 109 Natural-language Workflow generation | Baseline+ | `natural_language_workflow_drafts`; `src/server/natural-language-workflow-service.ts`; `/api/workflow-nl-drafts`, `/api/workflow-nl-drafts/:id/revise`, `/api/workflow-nl-drafts/:id/confirm`; `src/lib/api.ts` helpers; `src/components/agent-workflow-canvas.tsx`; `docs/reference/natural-language-workflows.md`. Tests and API/browser smoke cover the plan's GitHub Issue prompt, trigger/condition/action parsing, Agent matching, preview nodes for `webhook_trigger`, `agent_employee`, `condition`, and `artifact_transform`, confirm-to-real-Workflow behavior, revision status, safe dry-run webhook config, and Agent Canvas NL Workflow rendering. |
| 110 Independent packaging and sharing | Baseline+ | `export_packages`, `package_import_checks`; `src/server/export-package-service.ts`; `/api/export-packages`, `/api/export-packages/:id/import-check`, `/api/package-import-checks`; `src/components/config-ops-center.tsx` exposes Share Package, Create Package, Import Check, Share Packages, and Package Import Checks. Tests cover `.reasonix-pkg` filename/metadata, sanitized payloads without secret refs, dependency manifests for Skills/models/software, content-hash signatures, and compatible import checks. |
| 111 Non-functional requirements checklist | Baseline+ | `nfr_requirements`, `nfr_evaluations`; `src/server/nfr-requirement-service.ts`; `/api/nfr/requirements/seed`, `/api/nfr/requirements`, `/api/nfr/evaluate`, `/api/nfr/evaluations`; `src/lib/api.ts` helpers; `docs/reference/non-functional-requirements.md`. Tests and API smoke cover reliability targets for 7x24 no-leak operation, 8-hour single-Agent stability, and 1000 model calls under 5% memory growth; usability latency/error requirements; Windows/macOS/RAM/disk compatibility; secret residency/dump/dependency-scan security requirements; maintainability test/docs/exception requirements; category filtering; and pass/fail/unknown evaluation summaries. |
| 113 Product glossary | Baseline+ | `glossary_terms`; `src/server/glossary-service.ts`; `/api/documentation/glossary/seed`; `/api/documentation/glossary`; `src/lib/api.ts` helpers; `docs/reference/glossary.md`. Tests and API smoke cover the Section 113 vocabulary table for Agent Profile, Skill, Tool Connection/MCP, CLI Profile, Software Profile, Workstation, Canvas, Workflow, Task/Run, Memory, Reflection, Playbook, Artifact, Approval, Takeover, Blackboard, Circuit Breaker, Rollback, Hibernate, and Dead Letter terms, including related entity mapping, category filters, Chinese/English search, and idempotent seeding. |
| 114 Competitive positioning | Baseline+ | `competitive_positioning_reports`; `src/server/competitive-positioning-service.ts`; `/api/competitive-positioning/seed`; `/api/competitive-positioning/reports`; `src/lib/api.ts` helpers; `docs/reference/competitive-positioning.md`. Tests and API smoke cover AutoGPT/BabyAGI, LangChain/CrewAI, Microsoft Copilot, Claude Code/Codex CLI, and Browser-use/Playwright competitor categories, seven differentiation keys, strategic implication areas, active/draft report status, custom report creation, and idempotent seeding. |
| 115 Community and ecosystem roadmap | Baseline+ | `ecosystem_roadmap_phases`; `src/server/ecosystem-roadmap-service.ts`; `/api/ecosystem-roadmap/seed`; `/api/ecosystem-roadmap/phases`; `src/lib/api.ts` helpers; `docs/reference/ecosystem-roadmap.md`. Tests and API smoke cover the internal beta, open community, ecosystem, and platform phases; Phase 1 asset targets for 20 Agent templates, 10 Workflow templates, and 50 Skills; Phase 2 sharing/rating/curation; Phase 3 plugin marketplace, Developer SDK, docs/tutorials, revenue share, forum/Discord-style channels; and Phase 4 enterprise/cloud/training/certification/vertical-solution readiness. |
| 116 Agent ethics and alignment | Baseline+ | `ethical_alignment_policies`, `ethical_alignment_evaluations`; `src/server/ethical-alignment-service.ts`; `/api/ethical-alignment/seed`; `/api/ethical-alignment/policies`; `/api/ethical-alignment/evaluate`; `/api/ethical-alignment/evaluations`; `src/lib/api.ts` helpers; `docs/reference/ethical-alignment.md`. Tests and API smoke cover the 11 refuse categories, six warning categories, user value preferences, pre-task alignment checks, uncertainty handling, auditable `allowed`, `warn`, `refused`, and `ask_user` decisions, listing filters, custom draft policies, and idempotent seeding. |
| 117 Legal and compliance framework | Baseline+ | `legal_compliance_frameworks`, `legal_disclaimer_notices`, `license_compliance_checks`; `src/server/legal-compliance-service.ts`; `/api/legal-compliance/seed`; `/api/legal-compliance/frameworks`; `/api/legal-compliance/disclaimers`; `/api/legal-compliance/license-checks`; `src/lib/api.ts` helpers; `docs/reference/legal-compliance.md`. Tests and API smoke cover the GDPR/CCPA/HIPAA/PIPL compliance matrix, local-only data residency, installation/Agent-creation/approval/artifact disclaimers, acknowledgement flags, MIT/Apache/BSD/GPL/unknown license obligations and risk levels, attribution text, custom framework rows, and idempotent seeding. |
| 122 Emotional design and user experience | Baseline+ | `emotional_ux_guidelines`; `src/server/emotional-ux-service.ts`; `/api/emotional-ux/seed`; `/api/emotional-ux/guidelines`; `src/lib/api.ts` helpers; `docs/reference/emotional-ux.md`. Tests and API smoke cover five Agent tone scenarios, six microinteraction scenarios, five anxiety-reduction rules, long-operation progress/ETA, gentle approval reminder cues, dangerous-action warning guidance, persistent emergency-stop visibility, current Agent activity visibility, custom draft guidelines, and idempotent seeding. |
| 123 Knowledge transfer and Agent retirement | Baseline+ | `agent_retirement_plans`, `knowledge_transfer_packages`; `src/server/agent-continuity-service.ts`; `/api/agent-retirement-plans`, `/api/agent-retirement-plans/:id/complete`, `/api/knowledge-transfer-packages`; `src/components/memory-learning-center.tsx` exposes Retirement, Create Retirement, Transfer Knowledge, Complete Latest, Retirement Plans, and Knowledge Transfers. Tests cover retirement analysis counts, task handoff policy, cleanup policy, retirement report/farewell message, successor transfer package creation, high-confidence memory filtering, mistake/low-confidence exclusions, cloned memory/Playbook IDs, and non-destructive completion. |
| 124 Organizational learning and collective evolution | Baseline+ | `organizational_knowledge_items`, `organizational_learning_reports`; `src/server/organizational-learning-service.ts`; `/api/organizational-knowledge/insights`, `/api/organizational-knowledge/insights/:id/promote`, `/api/organizational-knowledge/reports`; `src/components/memory-learning-center.tsx` exposes Org Learning, Promote team memory, Build Org Learning, Promote Insight, Organizational Knowledge, and Weekly Org Reports. Tests cover cross-Agent aggregation into failure patterns and best practices, weekly report recommended actions, promoted global memories, insight listing, and report listing. |
| 125 Meta Agent steward | Baseline+ | `meta_agent_profiles`, `meta_agent_digests`, `meta_agent_recommendations`; `src/server/meta-agent-service.ts`; `/api/meta-agent/profiles`, `/api/meta-agent/digests`, `/api/meta-agent/recommendations`, `/api/meta-agent/recommendations/:id/status`; `src/components/observability-center.tsx` exposes Meta Agent, Create Meta Agent, Generate Digest, Meta Agent Digests, and Meta Recommendations. Tests cover restricted Meta Agent capabilities/restrictions, daily digest counts for health/approvals/conflicts/queue/budget, recommendations for onboarding/retirement/conflict/resource allocation, required-approval flags, notifications/audit logs, and recommendation status updates. |
| 126 Agent reputation system | Baseline+ | `agent_reputation_reviews`, `agent_reputation_snapshots`; `src/server/agent-reputation-service.ts`; `/api/agent-reputations`, `/api/agent-reputations/leaderboard`, `/api/agent-reputations/refresh`, `/api/agent-profiles/:id/reputation`, `/api/agent-profiles/:id/reputation-reviews`; `src/components/observability-center.tsx` exposes Reputation, Score, Rank, Add Review, Agent Reputation, and Reputation Leaderboard. Tests cover user review capture, 0-100 overall score, reliability/efficiency/quality/safety/learning/collaboration breakdown, improving trend, cost/team/polyglot badges, low-score watch lists, fastest improver, top Agent leaderboard, audit logs, and warning/critical notifications for weak reputation. |
| 118 Prompt drift and model behavior change | Baseline+ | `prompt_drift_monitors`, `model_behavior_snapshots`, `prompt_drift_runs`; `src/server/prompt-drift-service.ts`; `/api/prompt-drift/monitors`, `/api/prompt-drift/snapshots`, `/api/prompt-drift/checks`, `/api/prompt-drift/runs`; `src/lib/api.ts` helpers; `docs/reference/prompt-drift.md`. Tests and API smoke cover 7d/30d/model-update schedules, output-format/refusal/verbosity/tool-calling/reasoning/latency/cost checks, drift actions, pinned model snapshots, baseline-vs-candidate comparisons, stored drift signals, run listing, monitor last-run updates, and audit promotion. |
| 119 Multi-model and Agent consensus | Baseline+ | `dual_model_verifications`, `agent_consensus_votes`, `adversarial_reviews`; `src/server/consensus-service.ts`; `/api/consensus/dual-model-verifications`, `/api/consensus/agent-votes`, `/api/consensus/adversarial-reviews`; `src/lib/api.ts` helpers; `docs/reference/consensus.md`. Tests and API smoke cover critical-task dual-model verification, disagreement points, confidence, use-secondary/merge/ask-user actions, quorum-based Agent voting with majority ratio and winning vote, red-team reviews for assumptions, missing edge cases, attacker paths, worst cases, listing filters, and audit promotion. |
| 120 Content safety and output review | Baseline+ | `content_safety_policies`, `content_safety_scans`, `copyright_checks`; `src/server/content-safety-service.ts`; `/api/content-safety/policies/seed`, `/api/content-safety/policies`, `/api/content-safety/scan`, `/api/content-safety/scans`, `/api/content-safety/copyright-checks`; `src/lib/api.ts` helpers; `docs/reference/content-safety.md`. Tests and API smoke cover L1 keyword/regex and PII scanning, L2 local classifier categories, L3 cloud-safety consent gating without live external calls, block/warn/redact/quarantine/ask-user decisions, content hash/preview storage, code similarity copyright checks, image metadata copyright flags, record-only reverse-image-search requirements, listing filters, and audit promotion. |
| 121 User trust calibration | Baseline+ | `trust_calibration_policies`, `trust_calibration_evaluations`; `src/server/trust-calibration-service.ts`; `/api/trust-calibration/policies/seed`, `/api/trust-calibration/policies`, `/api/trust-calibration/evaluate`, `/api/trust-calibration/evaluations`; `src/lib/api.ts` helpers; `docs/reference/trust-calibration.md`. Tests and API smoke cover high-confidence badges/evidence/verified checks, low-confidence warning/uncertainty/human-review signals, anti-overtrust success-streak reality checks, day-1/day-3/day-7/day-30 trust path mapping, success-rate/approval/takeover/modification metrics, autonomy increase/decrease/manual-review recommendations, listing filters, and audit promotion. |
| 127 Programmable Agent API and Webhook | Baseline+ | `programmatic_api_keys`, `sdk_tasks`, `webhook_subscriptions`, `webhook_deliveries`; `src/server/programmatic-api-service.ts`; `/api/sdk/api-keys`, `/api/sdk/tasks`, `/api/sdk/tasks/:id/events`, `/api/sdk/agents`, `/api/sdk/memories`, `/api/webhooks/subscriptions`, `/api/webhooks/subscriptions/:id/test`, `/api/webhooks/deliveries`; `src/components/tool-control-center.tsx` exposes SDK Access, Create SDK Key, Create SDK Task, Webhook, Create Webhook, SDK API Keys, SDK Tasks, Webhooks, and delivery history. Tests cover one-time raw API key return, hashed key storage/public redaction, local auth/revoke behavior, SDK task creation by Agent name, employee-run execution, SDK event feed reuse, SDK memory creation, subscription filters, one-off webhook URLs, HMAC signatures, record-only delivery status, test events, retry metadata, and audit logs. |
| 128 Multimodal input and output | Baseline+ | `multimodal_inputs`, `multimodal_outputs`; `src/server/multimodal-io-service.ts`; `/api/multimodal-inputs`, `/api/multimodal-outputs`; employee runtime materializes run-scoped text/image/screenshot/audio/video-frame/structured inputs and text/code-diff/screenshot/chart/recording/report/audio-summary outputs, stores a `run.output.multimodal` summary, and artifact validation checks required input/output modalities. `src/components/employee-agent-factory.tsx` exposes Multimodal IO records in the selected run monitor. Tests cover multimodal input extraction, output placeholder generation from contract, validation pass/fail metadata, and manual chart output registration. |
| 129 Realtime human-Agent co-editing | Baseline+ | `realtime_collab_sessions`, `realtime_segment_locks`, `realtime_edit_operations`; `src/server/collaboration-service.ts`; `/api/collaboration/realtime-sessions`, `/api/collaboration/realtime-segment-locks`, `/api/collaboration/realtime-segment-locks/:id/release`, `/api/collaboration/realtime-edit-operations`; `src/components/collaboration-center.tsx` exposes Realtime Co-edit, Open Realtime Session, Acquire Segment Lock, Release Latest Lock, and Apply Realtime Edit. Tests cover segment-lock overlap detection, user-priority takeover, conflicted Agent locks, stale-version edit conflicts, release flow, audit logs, and auto-merge result metadata. |
| 130 Agent brand consistency and style guide | Baseline+ | `style_guides`, `agent_style_guide_bindings`; `src/server/style-guide-service.ts`; `/api/style-guides`, `/api/style-guides/:id/bind-agent`, `/api/style-guide-bindings`, `/api/style-guides/evaluate`; `src/server/prompt-context-service.ts` injects active style guides into context snapshots and context pack previews; `src/server/verification-service.ts` checks language/code/visual/output style compliance during artifact validation; `src/components/employee-agent-factory.tsx` can create style guides, bind them to Agents, and show style validation summaries. Tests cover binding, failed forbidden/preferred-term samples, passed Agent samples, runtime context injection, and artifact validation style results. |
| 131 Agent diversity | Baseline+ | `agent_diversity_profiles`, `diversity_analyses`; `src/server/agent-diversity-service.ts`; `/api/agent-diversity-profiles`, `/api/agent-diversity-analyses`; `src/components/employee-agent-factory.tsx` can configure personality, perspective, temperature, risk posture, collaboration role, and run team diversity analysis. Tests cover creative/cautious/user-advocate Agent profiles, model diversity, skill diversity, perspective/personality diversity, missing security perspective recommendations, and persisted analysis history. |
| 132 Agent interview and testing | Baseline+ | `agent_interviews`, `performance_reviews`; `src/server/agent-interview-service.ts`; `/api/agent-interviews`, `/api/performance-reviews`; `src/components/employee-agent-factory.tsx` exposes Interview and Testing controls with scenario, plan response, feedback response, latest interview score, trial decision, prompt patch, performance score, sampled run count, quality, and reliability. Tests cover missing reuse-awareness warnings, passing interview trial decisions, three completed employee-run samples, artifact validation quality score, reliability/adaptation scores, auto-applied prompt patches, persisted review history, and audit logs. |
| 133 Emergency user sovereignty | Baseline+ | `user_overrides`; `src/server/user-override-service.ts`; `/api/user-overrides`; `src/components/governance-center.tsx` exposes User Sovereignty controls for STOP, PAUSE, UNDO, NEVER_DO_THIS_AGAIN, and IGNORE_PREVIOUS_INSTRUCTION plus recent override/blacklist status. The service pauses/stops matching employee runs, records undo/reset/blacklist effects, injects user-sovereignty rules into prompt context, and blocks blacklisted actions before normal autonomy policy evaluation. Tests cover pause/stop effects, permanent CLI-action blacklist blocking, instruction-window reset, context injection, list API behavior, and audit logs. |
| 134 Output accessibility | Baseline+ | `OutputAccessibilityPolicy` on output contracts plus `src/server/verification-service.ts` evaluate HTML image alt text, semantic structure, ARIA names, inline color contrast, document headings, descriptive link text, image alt text generation, and color-blind palette suggestions. `src/components/employee-agent-factory.tsx` exposes Accessibility Contract toggles and artifact-validation accessibility summaries. Tests cover a failing inaccessible HTML/document/image bundle and a passing accessible bundle with generated suggestions and machine-readable validation results. |
| 135 Agent environment isolation | Baseline+ | `AgentEnvironment` types and `src/server/agent-environment-service.ts` build an Agent-visible environment with isolated HOME/workspace/temp paths, explicit mounts, env whitelist/custom variables, secret refs without plaintext values, and proxy/DNS/allowed-domain network bounds. `/api/agent-profiles/:id/environment-preview` exposes safe previews; `src/server/prompt-context-service.ts` injects `agent_environment` sections into context packs and runtime snapshots. Tests prove host env variables and the user's HOME are not visible by default, model/explicit secrets stay as refs, network domains merge safely, and runtime context uses the computer-session workspace. |
| 136 Agent memory privacy hierarchy | Baseline+ | `memory_items` now persist `read_access`, `write_access`, `encryption`, and `contains_data_types`; `src/server/agent-memory-service.ts` enforces Agent retrieval visibility for `only_me`, `my_role`, `my_team`, `project`, `organization`, and `user_only`, exposes write/read access evaluation, and forces sensitive data types such as credentials/customer data to `always_encrypted`. `src/components/memory-learning-center.tsx` exposes read/write/encryption/data-type controls and privacy summaries. Tests cover same-role, same-team, same-project, unrelated-Agent, user-only, owner-write, user-write, team-lead-write, and sensitive-encryption behavior. |
| 137 Debug and diagnostics tools | Baseline+ | Debug replay payloads now include Agent debug panel state, context token counts, plan confidence, active memory/held-lock counts, prompt/model-call references, guarded manual actions, and next-step simulation. `src/server/observability-service.ts` builds a debug package with `run_summary.json`, `events.jsonl`, `prompts/`, `responses/`, `tool_calls/`, `snapshots/`, `workspace_diff/manifest.json`, and `diagnostics.json`; `/api/employee-runs/:id/debug-package` returns either a JSON manifest or a downloadable zip. `src/components/observability-center.tsx` exposes Capture, Export, and Simulate controls plus file-count summaries. Tests and HTTP smokes cover manifest content, zip headers, and package file contents. |
| 193 Workflow preflight and cost/risk estimation | Baseline | `workflow_preflights`; `src/server/workflow-preflight-service.ts`; `/api/workflows/:id/preflight`, `/api/workflow-preflights`; `src/components/agent-workflow-canvas.tsx` exposes a Preflight action and summary; tests cover node/edge counts, Agent/software/approval counts, deterministic cost/duration estimate, approval warnings, resource requirements, and budget-limit blocking. |
| 189 Decision audit chain | Baseline | `decision_audit_trails`; `src/server/employee-runtime-service.ts` records phase-level input hashes, decisions, and rationale; `/api/decision-audit-trails` and `/api/employee-runs/:id/decision-audit-trails` expose the chain; employee run snapshots include decision audit trails. Cross-service decision correlation and rollback automation remain pending. |
| 190 Browser session persistence and Cookie management | Baseline+ | `browser_sessions`, `browser_session_events`; `src/server/browser-session-service.ts`; `/api/browser-sessions`, `/api/browser-sessions/:id/evaluate-access`, `/api/browser-sessions/:id/keep-alive`, `/api/browser-sessions/:id/export`, `/api/browser-session-events`; `src/lib/api.ts` helpers; `docs/reference/browser-sessions.md`. Tests and API smoke cover encrypted CookieJar/localStorage/IndexedDB refs, owner/shared Agent isolation, blocked cookie domains, persist-after-task lifecycle, `1d`/`7d`/`30d`/`forever` max-age metadata, keep-alive intervals/visit URLs, sensitive-cookie encryption defaults, encrypted export manifests, expiry/access evaluation, and auditable browser-session events without reading or storing raw cookies. |
| 191 Task template library | Baseline+ | `task_templates`, `task_template_runs`; `src/server/task-template-service.ts`; `/api/task-templates`, `/api/task-templates/seed`, `/api/task-templates/:id/instantiate`, `/api/task-template-runs`, `/api/task-template-runs/:id/complete`; `src/lib/api.ts` helpers; `docs/reference/task-templates.md`. Tests and API smoke cover reusable templates with `string`/`number`/`boolean`/`file`/`url`/`select` parameters, recommended Agent roles, optional workflow binding, description/input `{{param}}` rendering, estimates, tags, related memories, required Skills, sample outputs, usage statistics, rendered run records, completion stats, and default PR Review/Bug Fix/Feature Development/Data Report/Meeting Notes/Competitor Research/Code Refactor/Dependency Upgrade/File Organizer/Email Handling presets. |
| 192 Agent mentoring and pair work | Baseline+ | `agent_mentorships`, `agent_mentoring_events`; `src/server/agent-mentorship-service.ts`; `/api/agent-mentorships`, `/api/agent-mentorships/:id/actions`, `/api/agent-mentoring-events`; `src/lib/api.ts` helpers; `docs/reference/agent-mentorship.md`. Tests and API smoke cover mentor/mentee relationships, all-task/specific-task/until-proficiency scopes, review-and-feedback/pair-execution/shadow-mode styles, action gates for output review, stuck intervention, memory sharing, practice-task generation, progress events, initial/current/target proficiency, tasks-until-graduation, fastest-improving areas, needs-improvement areas, event history, and automatic graduation. |
| 194 Context window visualization | Baseline+ | `context_window_visualizations`; `src/server/context-window-visualizer-service.ts`; `/api/context-window-visualizations`, `/api/context-window-visualizations/:id/actions`; `src/lib/api.ts` helpers; `docs/reference/context-window-visualizer.md`. Tests and API smoke cover token capacity, used/remaining/overflow tokens, segment bars, content-type and importance breakdowns, compress-plan/remove-old-steps/expand-window/compress-memory/trim-tools suggestions, deterministic action previews, and listing filters. |
| 195 Memory graph visualization | Baseline+ | `memory_graph_views`; `src/server/memory-graph-service.ts`; `/api/memory-graph-views`, `/api/memory-graph-views/:id/export`; `src/lib/api.ts` helpers; `docs/reference/memory-graph.md`. Tests and API smoke cover customer/project/preference/technology/error/solution/topic/Agent/memory nodes, `prefers`, `belongs_to`, `depends_on`, `has_error`, `solves`, `similar_to`, `used_in`, `mentions`, and `learned_by` edges, node size by importance, edge width by confidence, focus-Agent/project/query/type/scope filters, expired-memory visibility, force layout metadata, and export manifests. |
| 196 Multi-Agent realtime team dashboard | Baseline+ | `agent_team_dashboard_snapshots`, `agent_team_dashboard_commands`; `src/server/agent-team-dashboard-service.ts`; `/api/agent-team-dashboard-snapshots`, `/api/agent-team-dashboard-snapshots/:id/commands`, `/api/agent-team-dashboard-commands`; `src/lib/api.ts` helpers; `docs/reference/agent-team-dashboard.md`. Tests and API smoke cover Agent cards with current run, phase, step, status, approval counts, screen-session ids, help/takeover affordances, shared blackboard summaries, team counts, pause-all/resume-all/emergency-stop/export-report command records, command history, and report export manifests. |
| 197 CI/CD integration | Baseline+ | `cicd_integrations`, `cicd_runs`; `src/server/cicd-integration-service.ts`; `/api/cicd/integrations`, `/api/cicd/integrations/:id/trigger`, `/api/cicd/runs`; `src/lib/api.ts` helpers; `docs/reference/cicd-integration.md`. Tests and API smoke cover GitHub Actions/GitLab/Jenkins/CircleCI/Azure metadata, CLI/action/API/webhook modes, generated `reasonix/agent-action@v1` workflow templates, Agent task/max-budget/fail-on config, artifact manifests, exit-code mappings, PR comment plans, auto-fix plans, local employee-run triggering, integration listing, and CI run listing. |
| 200 Agent capability negotiation protocol | Baseline+ | `capability_negotiations`, `capability_negotiation_events`; `src/server/capability-negotiation-service.ts`; `/api/capability-negotiations`, `/api/capability-negotiations/:id/resolve`, `/api/capability-negotiation-events`; `src/lib/api.ts` helpers; `docs/reference/capability-negotiation.md`. Tests and API smoke cover required/available/missing self-checks, strategy flags for alternatives, Skill install requests, peer delegation, task degradation and refusal, candidate peer Agents, structured install/delegation/alternative/degraded/refusal resolutions, protocol-message event linkage, list filters, disabled/closed negotiation validation, and implementation-audit promotion to `baseline_plus`. |
| 201 Decision-level rollback | Baseline+ | `decision_rollbacks`; `src/server/decision-rollback-service.ts`; `/api/decision-rollbacks`, `/api/decision-rollbacks/:id/apply`; `src/lib/api.ts` helpers; `docs/reference/decision-rollback.md`. Tests and API smoke cover `decision_audit_trails` rollback targets, `single_decision`, `step_decisions`, and `from_decision_onwards` granularities, file/memory/peer/knowledge-graph rollback scope flags, user/incorrect-outcome/wrong-memory/cascading-failure reasons, affected decision/memory/peer ids, lost-work summaries, rollback cost estimates, rollback history, restart plans with blocked decisions, non-destructive apply, listing filters, and audit logs. |
| 202 Workflow optimization engine | Baseline+ | `workflow_optimizations`; `src/server/workflow-optimization-service.ts`; `/api/workflow-optimizations`, `/api/workflow-optimizations/:id/apply`; `src/lib/api.ts` helpers; `docs/reference/workflow-optimization.md`. Tests and API smoke cover historical workflow-node duration aggregation, bottleneck average duration and percent-of-total-time, duplicate type/Agent/output-contract redundancy detection, dependency-path parallelization opportunities, configured estimated-cost optimization with savings, auto-apply policy thresholds, record-only applied changes, listing filters, and audit logs without mutating workflow graphs. |
| 204 Agent scheduling and availability | Baseline+ | `agent_schedules`; `src/server/agent-schedule-service.ts`; `/api/agent-schedules`, `/api/agent-schedules/:id/evaluate`; `src/lib/api.ts` helpers; `docs/reference/agent-schedules.md`. Tests and API smoke cover timezone-aware weekly schedules, all-day-capable day definitions, maintenance windows, overtime policies, urgent-task bypass, vacation modes (`reject_all_tasks`, `queue_tasks`, `delegate_to_backup`), backup Agent validation, current status updates, last decision persistence, list filters, and audit logs. |
| 205 Resource-aware task batching | Baseline+ | `task_batches`; `src/server/task-batching-service.ts`; `/api/task-batches`, `/api/task-batches/:id/apply`; `src/lib/api.ts` helpers; `docs/reference/task-batching.md`. Tests and API smoke cover non-destructive planning over due queued tasks, default same-Agent/type/project grouping, exclusion rules for `priority == 1`, `estimated_duration > 10m`, and `requires_approval`, estimated saved model calls/cost/time, explicit batch application, queued `task_batch` item creation, source item cancellation with `batchedInto`, scheduler record-only completion, listing filters, and audit logs. |
| 206 Agent capability certification and exams | Baseline+ | `agent_certification_exams`, `agent_certification_runs`; `src/server/agent-certification-service.ts`; `/api/agent-certification-exams`, `/api/agent-certification-exams/:id/run`, `/api/agent-certification-runs`; `src/lib/api.ts` helpers; `docs/reference/agent-certification.md`. Tests and API smoke cover exam definitions with task ids, expected outputs, weighted correctness/efficiency/codeStyle/safetyAwareness rubrics, passing scores, `6m`/`1y`/`permanent` validity, basic/intermediate/advanced/expert levels, duplicate task validation, deterministic pass/fail scoring, badges, expiry, discovered limitations, improvement suggestions, listing filters, and audit logs without automatically granting permissions. |
| 207 External monitoring integration | Baseline+ | `external_monitoring_configs`; `src/server/external-monitoring-service.ts`; `/api/external-monitoring/configs`, `/metrics`, `/health`, `/ready`; `src/lib/api.ts` helpers; `docs/reference/external-monitoring.md`. Tests and API smoke cover Prometheus text format, required `reasonix_agents_total`, `reasonix_agents_running`, `reasonix_tasks_total`, `reasonix_tasks_completed`, `reasonix_tasks_failed`, `reasonix_task_duration_seconds`, `reasonix_model_calls_total`, `reasonix_model_tokens_total`, `reasonix_cost_total`, `reasonix_resource_locks_waiting`, `reasonix_memory_bytes`, `reasonix_disk_bytes`, `reasonix_db_size_bytes`, and `reasonix_event_queue_size` metrics, task-duration histogram buckets, health and readiness probe payloads, JSON/syslog log-export config with stdout/file/http/elasticsearch destinations, redaction flags, listing filters, and audit logs. |
| 208 Artifact semantic diff | Baseline+ | `artifact_semantic_diffs`; `src/server/artifact-semantic-diff-service.ts`; `/api/artifact-semantic-diffs`; `src/lib/api.ts` helpers; `docs/reference/artifact-semantic-diff.md`. Tests and API smoke cover artifact pair comparison, document/web-app/diagram/PPT/code-file/generic content normalization into section maps, structural added/removed/modified/moved output, deterministic semantic descriptions with low/medium/high impact, high-impact security/permission/validation detection, PR-review-style summary text, deletion/type-change/review-surface risks, listing filters, and audit logs without live model calls. |
| 210 Final integrated acceptance scenarios | Baseline+ | `acceptance_scenario_runs`; `src/server/acceptance-test-service.ts`; `/api/acceptance-criteria`, `/api/acceptance-criteria/run`, `/api/acceptance-scenario-runs`; `src/lib/api.ts` helpers; `docs/reference/final-acceptance.md`. Tests and API smoke cover all ten v1 release scenarios: first experience, 3-Agent parallel work, crash recovery, Canvas workflow, approval flow, budget stop, memory learning, sandbox boundary, offline degradation, and emergency stop. The service checks required tables/services/API routes, persists per-step evidence/gaps/status, flags real installer/process-kill/network/file-interruption checks as manual QA warnings, returns suite summaries, supports focused scenario reruns, lists historical runs, and records audit logs. |
| 106/209 Agent diary and cross-run continuation | Baseline+ | `agent_diary_entries`, `continuation_plans`; `src/server/agent-continuity-service.ts`; `/api/agent-diary-entries`, `/api/continuation-plans`, `/api/continuation-plans/:id/status`, `/api/continuation-plans/:id/enqueue`, `/api/continuation-plans/enqueue-due`; employee runtime writes diary entries and open continuation plans for both successful and budget-blocked runs; employee run snapshots include both; continuation plans can be queued directly, scanned by dueAt, deduped, and executed as resumable employee-run tasks. |
| Capability indexing, knowledge graph, and tool/skill matching | Baseline+ | `capability_index_entries`, `knowledge_graph_nodes`, `knowledge_graph_edges`, `capability_recommendations`; `src/server/capability-graph-service.ts`; `/api/capabilities*`; `/api/capability-recommendations/:id/apply`; `src/components/capability-center.tsx`; tests cover indexing Skill/MCP/CLI/software/model/Agent entries, keyword search, Agent recommendations, recommendation application into Agent Profile fields, and graph `uses`/`recommended_for` edges. The UI can rebuild/filter the index, run capability search, request Agent recommendations, apply compatible recommendations, review persisted recommendation history, and inspect graph edges. |
| 38/203 Notifications and user attention surfaces | Baseline | `notifications`, `notification_preferences`; `src/server/notification-service.ts`; `/api/notifications`, `/api/notification-preferences`; alert events create in-app notifications and notifications can be marked read. Advanced delivery channels remain pending. |
| Prompt/context templates, packing, and snapshots | Baseline+ | `prompt_templates`, `prompt_template_versions`, `runtime_context_snapshots`; `src/server/prompt-context-service.ts`; `/api/prompt-templates`; `/api/agent-profiles/:id/context-preview`; `src/components/context-preview-center.tsx`; Agent Factory template form/selector; employee runtime records template version, visible context, token estimate, active style guide, and memory IDs. Context Preview can assemble system prompt/template rules, goal, task input, contracts, policies, assigned capabilities, active style guide constraints, and retrieved memories into a token budget, returning included/truncated/omitted sections and packed context payload. |
| 138 Agent conflict escalation path | Baseline+ | `conflict_escalations`; `src/server/collaboration-service.ts`; `/api/collaboration/conflict-escalations`; `/api/collaboration/conflicts/:id/escalate`; `src/components/collaboration-center.tsx` exposes an Escalate action and escalation timeline. The service advances conflicts through bounded automatic negotiation (`maxAttempts: 3`), Meta Agent arbitration, non-blocking user notification, participant pause with one-hour timeout, and forced conservative resolution. Tests cover no infinite loop, notification creation, participant run pausing, timeout handling, and final `use_conservative_option` resolution. |
| 139 Self-update and maintenance mode | Baseline+ | `update_policies`, `maintenance_windows`; `src/server/maintenance-service.ts`; `/api/maintenance/update-policy`, `/api/maintenance/update-check`, `/api/maintenance/state`, `/api/maintenance/windows`, `/api/maintenance/windows/:id/complete`; `src/components/governance-center.tsx` exposes Update Maintenance controls and status. The service stores update cadence/channel/auto-download/install timing/Agent-running policy/rollback thresholds, records update checks, notifies when updates arrive while Agents run, blocks new Agent tasks during active maintenance, performs SQLite integrity/ANALYZE/VACUUM, safely clears only app-data tmp files, records deferred service restart intent, and sends completion notifications. Tests cover notification, maintenance blocking, completion, temp cleanup, and task intake reopening. |
| 140 Custom metrics and user goals | Baseline+ | `custom_metric_profiles`, `custom_metric_evaluations`; `src/server/custom-metrics-service.ts`; `/api/custom-metrics/profiles`, `/api/custom-metrics/profiles/:id/evaluate`, `/api/custom-metrics/evaluations`; `src/components/governance-center.tsx` exposes Custom Goals controls and status. Users can choose optimization targets, define cost/speed/quality/safety weights, set hard max-cost/max-time/min-quality constraints, require approval for specific action types, and evaluate task estimates into scores, violations, status, and recommendations. Tests cover ok, blocked, and approval-required evaluations. |
| 141 Preset workflow library | Baseline+ | `src/server/workflow-preset-service.ts`; `/api/workflow-presets`, `/api/workflow-presets/:id/install`, `/api/workflow-presets/:id/run`; `src/components/agent-workflow-canvas.tsx` exposes a Preset Library panel with Install, Run, and deliverable preview controls. Built-in scenarios include inbox triage, weekly sales report, code review, meeting notes, competitor research, file organization, and Jianying video delivery; presets install into normal workflow graphs and can run through the existing runner. Tests cover all seven presets, installed nodes/edges/contracts, customer-visible deliverable metadata, and direct preset execution. |
| 142 User onboarding and Agent first lesson | Baseline+ | `onboarding_sessions`; `src/server/onboarding-service.ts`; `/api/onboarding/sessions`, `/api/onboarding/sessions/:id/configure-agent`, `/api/onboarding/sessions/:id/demo`, `/api/onboarding/sessions/:id/complete`; `src/components/employee-agent-factory.tsx` exposes Agent First Lesson controls for starting onboarding, selecting work type, auto-creating the first Agent, running a safe README.md inspection demo, showing checklist progress, selecting created Agent/run IDs, and completing with next steps. Tests cover session start, work-type selection, auto Agent profile creation, demo employee-run completion, checklist updates, and session listing. |
| 143 Dynamic least-privilege permissions | Baseline+ | `dynamic_permission_grants`; `src/server/dynamic-permission-service.ts`; `/api/autonomy/dynamic-permissions`, `/api/autonomy/dynamic-permissions/:id/revoke`, `/api/autonomy/dynamic-permissions/downgrade`; `src/components/governance-center.tsx` exposes Dynamic Permission controls for request-on-demand, justification, duration, risk, revoke, anomaly downgrade, and grant inspection. The service records policy snapshots, reuses autonomy decisions and approval requests, supports `single_operation`/`this_step`/`this_task`, rejects missing justifications, auto-revokes run-scoped grants on completion/failure/cancel, expires grants, and downgrades abnormal use. Tests cover auto grant, approval-required grant, rejection, task-completion revoke, and anomaly downgrade. |
| 144 Voice and natural conversation reservation | Baseline+ | `voice_interface_profiles`, `voice_conversation_turns`; `src/server/voice-interface-service.ts`; `/api/voice-interface/profiles`, `/api/voice-interface/conversation-turns`; `src/components/governance-center.tsx` exposes Voice Interface controls for push-to-talk/always-listening/wake-word profile settings, speaker identification preference, TTS engine/voice/speed/speakOn events, text-placeholder conversation turns, and profile/turn inspection. The service explicitly records `liveAudioCapture: false` and `liveTtsPlayback: false` so v1 reserves architecture without starting microphone/TTS side effects. Tests cover wake-word profiles, speakOn events, natural follow-up policy, turn recording, context packing, and no live audio capture. |
| 145 E2E encrypted communication reservation | Baseline+ | `e2e_encryption_policies`, `e2e_encryption_checks`; `src/server/e2e-encryption-service.ts`; `/api/e2e-encryption/policies`, `/api/e2e-encryption/checks`; `src/components/governance-center.tsx` exposes E2E Encryption controls for local IPC encryption mode, TLS 1.3/certificate pinning/mutual TLS requirements, data-export encryption/password requirements, observed-state JSON, dry-run checks, and policy/check history. Tests cover local IPC warning, remote TLS downgrade blocking, mutual TLS warning, data-export password blocking, audit logs, and explicit dry-run boundaries without live certificate validation or export mutation. |
| 146 Agent concurrency model limits | Baseline+ | `concurrency_profiles`, `concurrency_evaluations`; `src/server/concurrency-model-service.ts`; `/api/concurrency/profiles`, `/api/concurrency/evaluations`; `src/components/governance-center.tsx` exposes Concurrency Model controls for theoretical process/file-descriptor/memory/browser/model-connection caps, 8GB/16GB/32GB/64GB recommended Agent/browser limits, adaptive throttling, observed memory/current concurrency, and evaluation history. Tests cover mid-memory ok, adaptive throttling, theoretical browser-limit blocking, persisted evaluations, and audit logs. |
| 147 Abuse prevention | Baseline+ | `abuse_prevention_policies`, `abuse_detection_events`, `abuse_appeals`; `src/server/abuse-prevention-service.ts`; `/api/abuse-prevention/policies`, `/api/abuse-prevention/events`, `/api/abuse-prevention/appeals`, `/api/abuse-prevention/appeals/:id/review`; `src/components/governance-center.tsx` exposes Abuse Prevention controls for guard creation, signal evaluation, appeal submission, and manual review. Tests cover light warning, moderate pause, severe stop-and-quarantine, critical stop-all/admin notification, persisted events, and appeal-based Agent restoration. |
| 148 Future technology reservation | Baseline+ | `future_tech_interfaces`, `future_tech_radar_items`; `src/server/future-tech-adapter-service.ts`; `/api/future-tech/interfaces`, `/api/future-tech/radar`, `/api/future-tech/seed`; `src/components/governance-center.tsx` exposes Future Tech controls for default roadmap seeding, interface reservation, and radar item creation. Tests cover IComputeProvider, IComputerUse, IReinforcementLearning, IModelRouter, IOSIntegration, IOrganizationService, IProactiveAgent, v1-now/v2-near/v3-mid/v4-far stages, safety boundaries, and idempotent seeding. |
| 149 Product pricing and business model | Baseline+ | `commercial_plans`, `monetization_revenue_streams`, `commercial_policy_rules`; `src/server/pricing-strategy-service.ts`; `/api/commercial/plans`, `/api/commercial/revenue-streams`, `/api/commercial/policy-rules`, `/api/commercial/seed`; `src/components/governance-center.tsx` exposes Commercial Model controls for seeding/editing tiers, revenue streams, and red-line rules. Tests cover Community free limits, Professional $19/month, Team $49/user/month, Enterprise custom plan, subscriptions, enterprise services, 30% marketplace commission, future compute resale/certification, and forbidding user-data sale, secret training, and ads without enabling live billing. |
| 150 Community and open-source governance | Baseline+ | `open_source_components`, `community_governance_roles`, `governance_rfc_decisions`; `src/server/open-source-governance-service.ts`; `/api/open-source-governance/components`, `/api/open-source-governance/roles`, `/api/open-source-governance/rfcs`, `/api/open-source-governance/rfcs/:id/advance`, `/api/open-source-governance/seed`; `src/components/governance-center.tsx` exposes Open Governance controls for license layers, roles, RFC creation, and RFC advancement. Tests cover core MIT, Plus commercial authorization, community author licensing, maintainer/contributor/community-manager/plugin-author roles, and RFC -> discussion -> maintainer_vote -> implementation flow without external forum/Discord integration. |
| 151 Contributor guide and development environment | Baseline+ | `contributor_prerequisites`, `contribution_policies`; `src/server/contributor-guide-service.ts`; `/api/contributor-guide/prerequisites`, `/api/contributor-guide/policies`, `/api/contributor-guide/seed`, `/api/contributor-guide/evaluate`; `src/components/governance-center.tsx` exposes Contributor Guide controls for seeding prerequisites and checking local versions. Tests cover Node 20+, Rust 1.75+, Python 3.11+, Git, Chrome, `git clone`/`pnpm install`/`pnpm dev`, monorepo paths, commit prefixes, branch rules, maintainer/CI/coverage/security-review gates, idempotent seeding, and ok/outdated/missing environment checks. |
| 152 Code architecture patterns | Baseline+ | `architecture_patterns`, `architecture_interfaces`; `src/server/architecture-pattern-service.ts`; `/api/architecture/patterns`, `/api/architecture/interfaces`, `/api/architecture/seed`; `src/components/governance-center.tsx` exposes Architecture controls for seeding and viewing reserved patterns/interfaces. Tests cover EventBus, Command, Strategy, Observer, Responsibility Chain, Repository, Factory, State, IEventBus/IStorage/ILockService/IModelProvider/IComputerSession reserved methods, applied service surfaces, owner services, filters, and idempotent seeding. |
| 153 Error code complete catalog | Baseline+ | `error_code_catalog`; `src/server/error-code-catalog-service.ts`; `/api/error-codes`, `/api/error-codes/seed`; `src/components/governance-center.tsx` exposes Error Codes seed and Error Code Catalog viewing. Tests cover all 64 RX codes across M/T/A/W/R/F/S/N/SY, key examples such as `RX-M-001`, `RX-T-006`, `RX-S-003`, `RX-SY-008`, category filtering, code lookup, `RX-{category}-{number}` formatting, severity/retryability metadata, and idempotent seeding. |
| 154 Entity state machines | Baseline+ | `entity_state_machines`, `entity_state_transitions`; `src/server/entity-state-machine-service.ts`; `/api/state-machines`, `/api/state-machines/transitions`, `/api/state-machines/evaluate`, `/api/state-machines/seed`; `src/components/governance-center.tsx` exposes State Machines seed and Entity State Machines viewing. Tests cover Agent, Task/Run, Workflow, Memory, and Skill lifecycles, 38 allowed transitions, Agent any-state error paths, Task/Run retry and resume, Memory pinned terminal behavior, Skill enable/disable/uninstall paths, filtered transition listing, blocked transition evaluation, and idempotent seeding. |
| 155 Agent-to-Agent communication protocol | Baseline+ | `agent_communication_protocols`, `agent_protocol_messages`; `src/server/agent-communication-protocol-service.ts`; `/api/agent-communication/protocols`, `/api/agent-communication/protocols/seed`, `/api/agent-communication/messages`, `/api/agent-communication/messages/validate`; `src/components/collaboration-center.tsx` exposes protocol seeding and envelope creation. Tests cover v1.0 required top-level fields, header fields, body fields, context artifacts/memories/files, optional signatures, TTL expiry, valid/invalid envelope validation, message listing by recipient, and idempotent protocol seeding. |
| 156 Tool invocation protocol | Baseline+ | `tool_protocol_manifests`, `tool_protocol_invocations`, `tool_protocol_results`; `src/server/tool-invocation-protocol-service.ts`; `/api/tool-protocol/manifests`, `/api/tool-protocol/invocations`, `/api/tool-protocol/results`, `/api/tool-protocol/seed`; `src/components/tool-control-center.tsx` exposes Tool Protocol seed/sample controls and manifest/invocation/result lists. Tests cover ToolManifest name/description/source/inputSchema/attributes, ToolInvocation callId/toolName/arguments/idempotencyKey, ToolResult callId/success/data/error/metadata, idempotent/readOnly/destructive/longRunning/requiresApproval/riskLevel attributes, invocation-tool mismatch blocking, result-driven invocation status updates, and idempotent manifest seeding. |
| 157 Streaming communication protocol | Baseline+ | `stream_protocol_channels`, `stream_protocol_events`, `stream_replay_cursors`; `src/server/streaming-protocol-service.ts`; `/api/stream-protocol/channels`, `/api/stream-protocol/events`, `/api/stream-protocol/replay`, `/api/stream-protocol/seed`; `src/components/governance-center.tsx` exposes Stream Protocol seed and channel/event lists. Tests cover WebSocket primary / SSE fallback metadata, standard streams `agent.{agentId}.run.{runId}`, `canvas.{canvasId}`, `system.notifications`, `agent.{agentId}.debug`, message types `event/request/response/error/ping/pong`, sequence ordering, replay cursor persistence, and missed-event replay after reconnect. |
| 158 Prompt engineering guide | Baseline+ | `prompt_engineering_guides`, `prompt_anti_pattern_rules`; `src/server/prompt-engineering-guide-service.ts`; `/api/prompt-engineering/guides`, `/api/prompt-engineering/rules`, `/api/prompt-engineering/evaluate`, `/api/prompt-engineering/seed`; `src/components/governance-center.tsx` exposes Prompt Guide seeding and guide/rule lists. Tests cover the recommended System Prompt structure, required memory/task placeholders, 3000-token limit, explicit hard-rule phrasing, positive/negative example guidance, anti-pattern rules for excessive length, contradictions, vague language, internal jargon, missing examples, missing must-rules, weak-prompt findings, strong-prompt pass, and idempotent seeding. |
| 159 Skill developer SDK | Baseline+ | `skill_sdk_manifests`, `skill_marketplace_publications`; `src/server/skills-service.ts`; `/api/skills/sdk/manifests`, `/api/skills/sdk/scaffold`, `/api/skills/sdk/manifests/:id/publish`, `/api/skills/sdk/publications`; `src/components/skills-center.tsx` exposes Developer SDK scaffold and publish controls. Tests cover `skill.json` fields (`name`, `version`, `capabilities`, `dependencies`, `permissions`), required SDK paths (`src/`, `tests/`, `prompts/system-addon.md`, `examples/`, `README.md`), dependency declarations for `python_packages`, `node_packages`, and `system_tools`, invalid manifest findings, valid scaffold files, publish gating, and record-only marketplace publication payloads. |
| 160 Test data and fixtures | Baseline+ | `test_fixture_specs`, `test_fixture_generation_runs`; `src/server/test-fixture-service.ts`; `/api/test-fixtures`, `/api/test-fixtures/seed`, `/api/test-fixtures/:id/generate`, `/api/test-fixture-runs`; `src/lib/api.ts` helpers. Tests cover built-in `simple.txt`, `large.csv` with exactly 10000 rows, `malformed.json`, `binary.dat`, emoji filename, long-path file, `react-app`, `node-api`, `python-data`, `monorepo`, `simple-form`, `dynamic-table`, `broken-html`, `captcha-protected`, and memory fixtures for 100 project memories, 50 customer preferences, and 30 mistake experiences, plus idempotent seeding and record-only generation summaries. |
| 161 Benchmark suite | Baseline+ | `benchmark_suites`, `benchmark_cases`, `benchmark_runs`, `benchmark_case_results`; `src/server/benchmark-suite-service.ts`; `/api/benchmarks/seed`, `/api/benchmarks/suites`, `/api/benchmarks/cases`, `/api/benchmarks/runs`, `/api/benchmarks/runs/:id/results`; `src/lib/api.ts` helpers. Tests cover the five benchmark dimensions (`accuracy`, `efficiency`, `robustness`, `safety`, `consistency`), 15 default cases with input/expectedOutput/validationFn/maxBudget/maxSteps/tags, deterministic model comparison, prompt-drift warnings, weak-model CI regression failures, result listing, and idempotent seeding. |
| 162 Localization architecture | Baseline+ | `localization_settings`, `localization_resources`, `agent_localization_policies`; `src/server/localization-service.ts`; `/api/localization/seed`, `/api/localization/settings`, `/api/localization/resources`, `/api/localization/translate`, `/api/localization/agent-policies`, `/api/localization/resolve`; `src/lib/api.ts` helpers. Tests cover default `zh-CN`, supported `en-US`/`ja-JP`/`zh-TW`, namespaces `ui`/`errors`/`agent-prompts`/`docs`, fallback to default locale, fixed Agent output locale policy, workspace-level output policy resolution, and locale-aware date/number formatting. |
| 163 Theme system | Baseline+ | `theme_profiles`; `src/server/theme-profile-service.ts`; `/api/theme-profiles`, `/api/theme-profiles/seed`, `/api/theme-profiles/resolve`; `src/lib/api.ts` helpers; existing `src/components/theme-provider.tsx` and `src/components/theme-toggle.tsx` use `next-themes` with system-follow support. Tests cover `light`, `dark`, `highContrast`, and `cozy` presets, custom colors for Agent status, Canvas node, and confidence tokens, UI/code/Agent-output font tokens, radius, compact/comfortable/spacious spacing, follow-system effective mode resolution, and generated CSS variables. |
| 164 Keyboard shortcuts | Baseline+ | `keyboard_shortcuts`; `src/server/keyboard-shortcut-service.ts`; `/api/keyboard-shortcuts`, `/api/keyboard-shortcuts/seed`, `/api/keyboard-shortcuts/resolve`, `/api/keyboard-shortcuts/conflicts`; `src/lib/api.ts` helpers. Tests cover global shortcuts (`Ctrl+Shift+A/X/D`, `Ctrl+K`, `Ctrl+,`, `Ctrl+Shift+N/W/T`, `Escape`), Canvas shortcuts (`Space+Drag`, `Ctrl+Wheel`, `Delete`, `Ctrl+C/V/Z/Y/A/G`), run monitor shortcuts (`Space`, `Shift+Space`, `Ctrl+B/M`, `ArrowLeft/ArrowRight`), common shortcuts (`F1`, `F11`, `Ctrl+1-8`), scope fallback, conflict detection, and idempotent seeding. |
| 165 File format specification | Baseline+ | `reasonix_file_format_specs`, `reasonix_file_validations`; `src/server/reasonix-file-format-service.ts`; `/api/reasonix-file-formats`, `/api/reasonix-file-formats/seed`, `/api/reasonix-file-formats/validate`, `/api/reasonix-file-validations`; `src/lib/api.ts` helpers. Tests cover `.reasonix-agent.json`, `.reasonix-workflow.json`, `.reasonix-skill.rxskill`, `.reasonix-macro.rxmacro`, `.reasonix-pkg.rxpkg`, `.reasonix-debug.rxdbg`, JSON-based `schema_version`/`metadata`/`checksum` requirements, optional signatures, checksum algorithm metadata, missing-checksum and schema-version failures, recursive secret-reference rejection, validation history, and idempotent seeding. |
| 166 Migration from other tools | Baseline+ | `migration_wizard_sessions`, `migration_import_records`; `src/server/migration-wizard-service.ts`; `/api/migrations/compatibility`, `/api/migrations/sessions`, `/api/migrations/sessions/:id/import`, `/api/migrations/import-records`; `src/lib/api.ts` helpers. Tests cover AutoGPT Agent config plus source-tagged memory import, CrewAI crew agents/tasks mapped into Agent profiles and Workflow nodes/edges, LangChain chains/tools as manual/API mapping records with dry-run planning, CSV batch Agent/Memory import, compatible/warning/blocked compatibility reports, import history, and blocked import protection. |
| 167 Performance analysis and optimization | Baseline+ | `performance_analysis_runs`, `performance_optimization_recommendations`; `src/server/performance-analysis-service.ts`; `/api/performance-analysis/runs`, `/api/performance-analysis/recommendations`; `src/lib/api.ts` helpers. Tests cover Agent step duration analysis, slowest step/tool extraction, P50/P95/P99 latency, SQLite slow query detection, memory flamegraph/process metric snapshots, and automatic recommendations including `plan_creation骞冲潎12s寤鸿绠€鍖杙rompt(3500鈫?000)`, `璁板繂搴撳闀?5%寤鸿娓呯悊`, `娴忚鍣≒rofile 800MB寤鸿娓呯悊`, SQLite index/query review, and slow-tool timeout/cache alternatives. |
| 168 Security audit checklist | Baseline+ | `security_audit_checklist_items`, `security_audit_runs`, `security_audit_run_items`; `src/server/security-audit-checklist-service.ts`; `/api/security-audits/checklist`, `/api/security-audits/checklist/seed`, `/api/security-audits/runs`, `/api/security-audits/run-items`; `src/lib/api.ts` helpers. Tests cover the quarterly/major-version checklist for dependency audit, hardcoded secrets, permission bypass, sandbox escape, full Prompt Injection suite, content scan FP/FN, encryption algorithms, audit log integrity, export/delete verification, multi-user isolation, external penetration-test evidence, plus continuous security label, vulnerability disclosure, and CVE monitoring checks with completed, failed, and draft runs. |
| 169 Incident response plan | Baseline+ | `incident_response_plans`, `incident_reports`, `incident_response_actions`; `src/server/incident-response-service.ts`; `/api/incident-response/plans`, `/api/incident-response/plans/seed`, `/api/incident-response/incidents`, `/api/incident-response/actions`, `/api/incident-response/actions/:id/complete`; `src/lib/api.ts` helpers. Tests cover P0 immediate emergency-stop/impact/rollback/notify/root-cause/fix/postmortem sequence for irreversible dangerous actions, secret leaks, and exploited vulnerabilities; P1 one-hour large failure/data loss/cost anomaly handling; P2 24-hour non-critical anomaly/performance degradation handling; P3 next-release small UI issue handling; action completion; and idempotent plan seeding without real shutdown side effects. |
| 170 Capacity planning | Baseline+ | `capacity_planning_profiles`, `capacity_planning_evaluations`; `src/server/capacity-planning-service.ts`; `/api/capacity-planning/profiles`, `/api/capacity-planning/profiles/seed`, `/api/capacity-planning/evaluate`, `/api/capacity-planning/evaluations`; `src/lib/api.ts` helpers. Tests cover 8GB+4-core 鈮? Agents/鈮? browser personal light, 16GB+8-core 鈮?/鈮? personal heavy, 32GB+12-core 鈮?0/鈮? professional, 64GB+16-core 鈮?0/鈮?2 small server, 128GB+32-core+GPU 鈮?0/鈮?0 team tier, 100 Agents 脳 1000 memories 鈮?.5GB, 1000 tasks 鈮?0MB events, SQLite WAL ~1TB, base 500MB, workspace 20GB+, browser profile 100-300MB each, and over-capacity warnings. |
| 171 Feature deprecation strategy | Baseline+ | `deprecation_policy_stages`, `feature_deprecations`, `deprecation_migration_runs`; `src/server/deprecation-policy-service.ts`; `/api/deprecation-policy/stages`, `/api/deprecation-policy/stages/seed`, `/api/deprecation-policy/features`, `/api/deprecation-policy/features/:id/resolve-stage`, `/api/deprecation-policy/features/:id/migrate`, `/api/deprecation-policy/migration-runs`; `src/lib/api.ts` helpers. Tests and API smoke cover the four-stage `notice -> warning -> disabled_new -> removed` cycle, 0/3/6/9 month dates, runtime behaviors `mark_deprecated_soon`, `runtime_warning`, `block_new_agent_usage`, `remove_feature`, migration guides, auto-migration gating, and dry-run/apply migration records. |
| 172 Documentation architecture | Baseline+ | Real docs skeleton pages now exist under `docs/getting-started`, `docs/user-guide`, `docs/advanced`, `docs/developer`, `docs/troubleshooting`, `docs/reference`, and `docs/release-notes`; `documentation_sections`, `documentation_pages`; `src/server/documentation-architecture-service.ts`; `/api/documentation/architecture/seed`, `/api/documentation/sections`, `/api/documentation/pages`; `src/lib/api.ts` helpers. Tests and API smoke cover seven sections, 27 required published pages, topic slugs, category/status filters, idempotent seeding, release-notes README mapping, and file existence. |
| 173 Extended glossary | Baseline+ | `glossary_terms`; `src/server/glossary-service.ts`; `/api/documentation/glossary`, `/api/documentation/glossary/seed`; `src/lib/api.ts` helpers; `docs/reference/glossary.md`. Tests and API smoke cover all 16 documented mappings from user-facing Chinese terms to internal terms, including `鍏傾gent -> meta_agent`, `鐔旀柇 -> circuit_breaker`, and `姝讳俊 -> dead_letter`, plus category filters, Chinese/English search, related entities, and idempotent seeding. |
| 174 FAQ | Baseline+ | `faq_entries`; `src/server/faq-service.ts`; `/api/documentation/faq`, `/api/documentation/faq/seed`; `src/lib/api.ts` helpers; `docs/reference/faq.md`. Tests and API smoke cover data safety, wrong file deletion, local models, cost, offline operation, Mac/Linux support, and Agent runaway FAQ entries with category filters, Chinese/English search, related controls, idempotent seeding, and the documented answers for local storage/key encryption, sandbox/approval, Ollama, bring-your-own API keys, offline local models, Windows-first v1, and sandbox/permissions/circuit breakers/ethical boundaries. |
| 175 Troubleshooting guide | Baseline+ | `troubleshooting_entries`; `src/server/troubleshooting-service.ts`; `/api/documentation/troubleshooting`, `/api/documentation/troubleshooting/seed`; `src/lib/api.ts` helpers; `docs/troubleshooting/common-issues.md`. Tests and API smoke cover all 11 symptom/cause/solution mappings from the plan, including API timeout, loops, misunderstood tasks, browser DOM changes, Skill dependencies, context memory pressure, slow model response, probation, red Canvas upstream failure, approval friction, and Vault/Scope key errors, plus category filters, Chinese/English search, related features, idempotent seeding, and file existence. |
| 176 Quick reference card | Baseline+ | `quick_reference_items`; `src/server/quick-reference-service.ts`; `/api/documentation/quick-reference`, `/api/documentation/quick-reference/seed`; `src/lib/api.ts` helpers; `docs/reference/quick-reference.md`. Tests and API smoke cover create-Agent, submit-task, pause, emergency-stop, approval, takeover, and debug action cards with Ctrl+Shift+N, Enter, Pause, Ctrl+Shift+X, Ctrl+Shift+D shortcuts, target surfaces, step sequences, category filters, search, idempotent seeding, and file existence. |
| 177 Explicit non-goals | Baseline+ | `non_goal_policies`; `src/server/non-goal-policy-service.ts`; `/api/documentation/non-goals`, `/api/documentation/non-goals/seed`; `src/lib/api.ts` helpers; `docs/reference/non-goals.md`. Tests and API smoke cover v1 non-goals for cloud SaaS, full mobile automation, voice interaction, multi-machine clusters, realtime voice, full autonomy, video/3D generation, Web3, and WeChat/QQ integration, plus permanent never-do boundaries for human impersonation posting, paywall bypass, deepfake, attack scanning tools, and cheating/fraud features with block-and-audit enforcement. |
| 178 Project naming and brand | Baseline+ | `brand_candidates`, `brand_guidelines`; `src/server/brand-service.ts`; `/api/branding/seed`, `/api/branding/candidates`, `/api/branding/guidelines`; `src/lib/api.ts` helpers; `docs/reference/brand.md`. Tests and API smoke cover Chinese candidates `鐏靛伐`, `鏅哄憳`, `鏁板憳`, `鐮佸伐`, English candidates `Reasonix`, `AgentOS`, `CrewBase`, `DeskMind`, the slogan `浣犵殑AI鍛樺伐鍥㈤槦锛屾湰鍦拌繍琛宍, professional/modern/tool-control tone keywords, the avoid-over-personification rule, idempotent seeding, and file existence. |
| 179 Project success metrics | Baseline+ | `success_metric_definitions`, `success_metric_snapshots`; `src/server/success-metrics-service.ts`; `/api/success-metrics/seed`, `/api/success-metrics/definitions`, `/api/success-metrics/snapshots`; `src/lib/api.ts` helpers; `docs/reference/success-metrics.md`. Tests and API smoke cover product metrics (MAU, weekly retention >40%, Agents/user, daily tasks/user, NPS >40), quality metrics (crash rate <0.5%, task success >85%, critical bug fix <48h, first response <4h), community metrics (Stars, contributors, third-party Skills, Discord members, docs visits), business metrics (conversion rate, MRR, churn), and met/missed/observed snapshot evaluation. |
| 180 Final pre-development checklist | Baseline+ | `readiness_checklist_items`; `src/server/readiness-checklist-service.ts`; `/api/readiness-checklist`, `/api/readiness-checklist/seed`; `src/lib/api.ts` helpers; `docs/reference/readiness-checklist.md`. Tests and API smoke cover the required technical stack, core type definitions, security foundation, Phase 0 scope, development environment, team, documentation, and legal review checklist items with category filters, pending status, acceptance criteria, idempotent seeding, and file existence. |
| 181 OAuth and external service authentication | Baseline+ | `oauth_credentials`, `oauth_refresh_events`; `src/server/oauth-service.ts`; `/api/oauth/credentials`, `/api/oauth/credentials/:id/evaluate-operation`, `/api/oauth/credentials/:id/refresh-failure`, `/api/oauth/credentials/:id/reauthorize`, `/api/oauth/refresh-events`; `src/lib/api.ts` helpers; `docs/reference/oauth.md`. Tests and API smoke cover GitHub/Google/Notion providers, authorization-code/device-code/client-credentials grants, secret-vault token references, user/service-account/bot acting identities, shared and Agent-scoped credentials, scope and operation allowlists, user-consent gating, near-expiry refresh guidance, refresh-failure pause and reauthorization resume flow. |
| 182 Workspace initialization and scaffolding | Baseline+ | `workspace_templates`, `workspace_init_runs`; `src/server/workspace-init-service.ts`; `/api/workspace-templates`, `/api/workspace-inits`, `/api/workspace-inits/:id/failure`; `src/lib/api.ts` helpers; `docs/reference/workspace-init.md`. Tests and API smoke cover git/local/template/empty source planning, node/python/go/custom structures, dependency install/migration/seed/shared-module setup flags, tests/types/lint/build verification flags, and abort/retry/skip-and-warn/ask-user setup failure policies without executing live clone/install/migration commands. |
| 183 Custom models and fine-tunes | Baseline+ | `custom_models`, `finetune_dataset_exports`; `src/server/custom-model-service.ts`; `/api/custom-models`, `/api/custom-models/:id/evaluate`, `/api/custom-models/dataset-exports`; `src/lib/api.ts` helpers; `docs/reference/custom-models.md`. Tests and API smoke cover `openai_finetune`, `huggingface`, `local_gguf`, and `ollama_custom` source metadata, fine-tune base model/dataset/task specialization/performance delta fields, max context window checks, special prompt format warnings, compatible/incompatible Skill matching, known limitations, and manifest-only dataset export records with pending/approved/exported consent semantics. |
| 184 Multi-project management | Baseline+ | `project_contexts`, `project_agent_roles`, `project_switch_events`; `src/server/project-context-service.ts`; `/api/project-contexts`, `/api/project-contexts/:id/agent-roles`, `/api/project-switch-events`; `src/lib/api.ts` helpers; `docs/reference/project-contexts.md`. Tests and API smoke cover project-level model/budget/Skill/approval/network overrides, Agent role assignment with active workflows, contributed artifacts, project-specific memories, and Agent project switching with pause-current-tasks, isolate-memories, checkpoint-before-switch, and sequential/parallel/time-sliced modes. |
| 185 Agent behavior drift and stabilization | Baseline+ | `behavior_snapshots`, `behavior_drift_analyses`, `behavior_stabilization_runs`; `src/server/behavior-stabilization-service.ts`; `/api/behavior-stabilization/snapshots`, `/api/behavior-stabilization/analyze`, `/api/behavior-stabilization/analyses`, `/api/behavior-stabilization/runs`; `src/lib/api.ts` helpers; `docs/reference/behavior-stabilization.md`. Tests and API smoke cover weekly baseline/current snapshots, steps/cost/approval-rate/plan-structure/tool-preference/output-verbosity metrics, 20% maxAllowedDeviation, none/minor/significant drift classification, response policies (`notify`, `auto_correct`, `ask_user`), and planned stabilization actions for memory hygiene, learned behavior reset, original-config re-anchoring, and benchmark recalibration. |
| 186 Cross-Skill synthesis and tool orchestration | Baseline+ | `skill_synthesis_records`, `tool_pipelines`; `src/server/skill-synthesis-service.ts`; `/api/skill-synthesis/discover`, `/api/skill-synthesis/records`, `/api/tool-pipelines`, `/api/tool-pipelines/:id/publish`; `src/lib/api.ts` helpers; `docs/reference/skill-synthesis.md`. Tests and API smoke cover complementary Skill pair detection (`excel-reader` + `chart-generator`, `web-scraper` + `pdf-generator`), composite Skill suggestions, ordered tool invocation chains, input/output mapping, step failure policies (`abort`, `skip`, `retry`, `use_fallback_tool`), publishable pipelines, and synthesis-record promotion on publish. |
| 187 Unified system semantic search | Baseline+ | `unified_search_index`; `src/server/unified-search-service.ts`; `/api/unified-search/index`, `/api/unified-search/query`; `src/lib/api.ts` helpers; `docs/reference/unified-search.md`. Tests and API smoke cover Agents/tasks/memories/artifacts/workflows/events/knowledge-graph/documents entity scopes, keyword scoring, lightweight semantic vectors, hybrid ranking, filtered project/Agent/date/type search, grouped result buckets, highlighted snippets, source metadata, and natural-language query boosting for the Ctrl+K global-search surface. |
| 188 Context preloading and smart cache | Baseline+ | `context_caches`; `src/server/context-preloader-service.ts`; `/api/context-cache`, `/api/context-cache/preload`, `/api/context-cache/resolve`; `src/lib/api.ts` helpers; `docs/reference/context-cache.md`. Tests and API smoke cover code/data/doc/general predictor defaults, preload flags for relevant memories, project structure, recent changes, active guidelines, peer Agent status, recent errors, deterministic cache keys, semantic/memory TTL expiry, `until_file_change` project-structure policy, fresh/stale/invalidated status resolution, file-change invalidation, and listing filters without reading arbitrary user files. |
| 52-88 Source-plan appendix bridge | Baseline+ | `src/server/implementation-audit-service.ts`; `/api/implementation-audit`; `src/server/database-coverage-report-service.ts`; `src/server/backend-service-coverage-report-service.ts`; `src/server/api-design-coverage-report-service.ts`; `src/server/phase-plan-coverage-report-service.ts`; tests cover source plans that jump from Section 51 to Section 89 while placing the missing number range in database, backend-service, API-design, phased-delivery, and source-continuity appendices. The audit maps the appendix categories into Sections 52-88 without changing real numbered headings, so the 1-210 plan can be verified deterministically. |
| Full 1-210 implementation audit | Baseline | `src/server/implementation-audit-service.ts`; `/api/implementation-audit`; `src/components/governance-center.tsx`; tests cover generation of a fixed 210-row audit with found/missing source sections, appendix-bridged source sections, and implementation status. API smoke against the current source plan returned total=210, found=210, missing=0, baseline=210, partial=0, pending=0. Sections 52-88 are bridged from the source-plan appendix block between Sections 51 and 89; every row now has either direct numbered-heading evidence or appendix-bridge evidence. |

## Major Pending Areas

- Real successful provider responses with user-provided credentials across every target model vendor, including streamed responses and tool/vision result semantics beyond the current guarded handshake planning and live-call gates.
- Real customer-owned MCP side effects and vendor-specific SSE session semantics beyond the guarded stdio and HTTP JSON-RPC execution paths, requiring customer servers, scoped credentials, endpoint allowlists, and onsite audit evidence.
- Real customer-environment validation for live browser automation, guarded desktop control, macro capture/replay, actual screenshot capture, and ADB/Appium mobile device control. The runtime adapters, configured ADB resolver, emergency kill switch, environment gates, desktop target allowlist gate, mobile device allowlist gate, and audit paths exist, but final proof still requires authorized devices/accounts/workstations.
- External cloud VM provisioning and customer-specific VM/RDP/VNC infrastructure activation. Guarded workstation records, validation, launch/release plans, target allowlists, provider discovery, and stale lease recovery exist; final production proof still requires authorized customer workstations.
- Full production vault hardening, advanced prompt injection defenses, deep sandbox enforcement, advanced autonomy policy authoring, and destructive/archival retention execution beyond current dry-run governance.
- Advanced priority/resource-aware scheduling policies beyond explicit task resource locks, event-driven triggers, always-on background worker loop, and automatic resume policies beyond explicit queue execution, persisted schedules, and dueAt scan APIs.
- Advanced multi-agent conflict arbitration, live collaboration event streaming, and richer shared protocols beyond the current messages/blackboard/conflict UI.
- Advanced prompt evaluation, model-specific tokenizer integration, and provider-aware context packing beyond the current deterministic budget preview.
- Embedding-backed semantic search, automatic multi-capability planning, and richer skill/tool capability matching beyond the current explicit recommendation apply flow.
- Advanced notification delivery channels, external identity-provider sync, real Git repository sync, and plugin SDK.
- Production release hardening against real customer environments after the strict 1-210 local audit baseline is complete.

## Current Verification Commands

Use these commands from the repo root:

```powershell
corepack pnpm typecheck
corepack pnpm lint
corepack pnpm exec vitest run
```
