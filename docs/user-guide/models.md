# Models

Purpose: document model profile setup and validation.

Scope:
- Provider, base URL, API key reference, capabilities, fallback models, and connection tests.
- Network outlet selection for model traffic.
